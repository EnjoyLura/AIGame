# AGENTS.md

本文件约束所有在本仓库内工作的 AI 与自动化开发代理。产品总纲见 `docs/PRD.md`。

## 1. 当前技术基线

- 引擎：Cocos Creator 3.8.8。
- 语言：TypeScript。
- 目标平台：微信小游戏。
- 屏幕方向：竖屏。
- 当前阶段：基础空项目，必须按 PRD 里程碑推进。

## 2. 工作原则

1. 开始任务前先阅读本文件和 `docs/PRD.md`。
2. 一次任务只服务于当前里程碑和明确需求，不提前实现未来系统。
3. 未提供具体功能需求文档时，只搭建当前验收所需的最小能力。
4. 优先遵循现有目录、命名、组件和配置方式，不进行无关重构。
5. 战斗、英雄、技能、怪物、增益和关卡必须面向数据驱动设计。
6. 逻辑正确性先于正式 UI 和美术表现。
7. 不复制《向僵尸开炮》或其他产品的素材、文本、角色、技能名称、数值和界面。
8. 每次变更必须附带可重复执行的验证结果。

## 3. 里程碑门禁

- M0 未通过前，不开发战斗 UI 和正式玩法。
- M1 未通过前，不接入正式美术资源。
- M2 未通过前，不扩展四英雄、多关卡和局外系统。
- M3 未通过前，不批量制作英雄、怪物和关卡内容。
- M4 未通过前，不开始商业化规模的内容扩展。
- M5 未通过前，不接入付费、广告和复杂运营活动。
- 后续里程碑必须以前一阶段的可运行版本为基础，不能用文档完成代替运行验收。

## 4. UI 约束

- 使用 `750 × 1334` 作为推荐逻辑设计尺寸，并适配安全区和长屏。
- 战斗中敌人只从上方主战区进入，英雄和运输载具固定在下方。
- 敌群生成与战斗区固定占据垂直空间约 7% 至 80%。
- 战斗界面左侧固定放置圆形全局战斗控制，包括暂停、倍速、自动开关及必要的辅助控制；左侧不得放置英雄技能。
- 战斗界面右侧只放置圆形战场环境技能按钮；除此之外不得堆叠长期功能按钮。
- 普通攻击不提供手动瞄准或攻击按钮。
- 四个英雄槽位尺寸和位置固定。
- 每名英雄的技能与大招圆形按钮紧邻该英雄站位显示，必须展示冷却或充能遮罩和可释放状态；自动状态只由战斗区左侧的全局自动开关显示和切换。
- 左侧控制、右侧环境技能和英雄旁能力按钮不得遮挡敌群主路径、Boss 预警和运输载具生命条。
- 纯色占位阶段必须完成真实布局验证，不能以未来换美术为由接受重叠、裁切和层级混乱。
- 禁止大面积紫色或蓝紫渐变、装饰性光球、过度圆角卡片和卡片嵌套。
- 正式美术替换不得随意改变已验证的信息结构。

## 5. 工程约束

- 不直接修改 Cocos 自动生成目录中的内容，包括 `library/`、`temp/` 和构建产物。
- 新增源码、场景和资源放在 `assets/` 下的明确业务目录。
- 不将高频生成和销毁的战斗对象保留为无池化实现。
- 不在单个组件中同时承担配置、战斗计算、表现、存档和 UI 职责。
- 不把关卡波次、敌人属性和技能数值硬编码在场景脚本中。
- 随机逻辑应支持注入或固定种子，以便复现问题。
- 新增公共接口时保持最小表面积，并说明使用方。
- 不引入新依赖，除非现有引擎能力无法合理完成任务，并在变更说明中写明原因。

## 6. 验证要求

每次开发完成后，至少执行与变更相关的以下检查：

- TypeScript 编译或 Cocos 脚本检查。
- 项目可打开、场景可运行。
- 当前里程碑的手动冒烟流程。
- 关键纯逻辑模块的自动验证。
- UI 变更的标准竖屏和长屏截图检查。
- 战斗变更的重复运行、暂停、重开和退出检查。
- 性能敏感变更的同屏压力检查。

无法执行某项验证时，必须明确说明未验证项、原因和风险。

## 7. 任务交付格式

完成任务时说明：

- 修改了什么。
- 修改了哪些文件。
- 如何验证。
- 验证结果。
- 已知限制或尚未覆盖的内容。
- 当前里程碑是否达到验收条件。

不得声称未实际运行的测试已经通过。

## 8. 文档约束

- 总体产品方向只修改 `docs/PRD.md`。
- 具体功能进入开发前再创建对应需求文档。
- 功能文档必须说明目标、范围、非目标、依赖、交互、异常和验收。
- 需求变更必须同步更新受影响的验收标准。
- 不在多个文档重复维护同一组详细规则。

## 9. Git 与文件安全

- 不撤销或覆盖用户已有的未提交修改。
- 不使用破坏性 Git 命令。
- 不提交缓存、日志、临时文件和自动生成产物。
- 修改 `.gitignore` 前先确认不会误排除源文件或必要配置。
- 大规模移动、删除或重命名前先核对实际引用关系。
