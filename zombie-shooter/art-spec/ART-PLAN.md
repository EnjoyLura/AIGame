# 游戏美术资源阶段计划（ART-PLAN）

> 2026-09-20 v2。方法论（用户定调）：**先按游戏 UI 归纳出所需 UI 组件清单 → 按 UI 分类分批
> 生成同类美术资源**（按钮类/图标类/面板类……），一族一张 2K 绿幕表一次成型。
> `game_art_benchmark2.png` 本身就是这份清单的布局示范，其中「框/徽」类件可直接取用。

## 0. 总纲

- **方法**：组件清单 → 按类分批 → 2K 绿幕大表一次成型（15~25 格）→ gpt-image-2 edit 模式
  锚定基准图2 → prompt 只报槽位名、不指定形状颜色 → `slice_sheet.py` 切片落盘 → 换图不换 key。
- **风格锚**：`game_art_benchmark2.png` 为唯一基准（casual 基准图仅作面板/按钮参考）。
- **放弃策略（冻结不清盘）**：现有在库图降级为「临时版」继续服役，各批次成套整族替换；
  缺图自动回退占位兜底，游戏全程可玩。
- **每批收尾**：目检过 §6 验收清单 → 切片落盘 → MANIFEST 登记 → 构建 → check 链 →
  新端口烟测 → 提交推送（一族一个 commit，可整体回退）。

## 1. UI 组件清单（归纳自游戏实际界面）

> 来源：主城 DOM（HomeUi 五页 + 弹层体系）与战斗 HUD（DomHud）。标 × 的是当前 emoji/CSS
> 占位、待贴图化的组件——这就是生成清单的来源。

### 1.1 弹层骨架（全游戏通用）
| 组件 | 现状 | 备注 |
|---|---|---|
| 浮窗底板 S/M/L/XL | CSS 渐变 | 需九宫格面板贴图（四角等宽契约） |
| 标题条 / 横幅 | CSS | 绶带需 `--rect` 自然比例切片 |
| 关闭钮 ✕ / 返回钮 ‹ | ✕ 已贴图(r4) / ‹ CSS | 小圆钮族 |
| 遮罩 | CSS | 无需贴图 |

### 1.2 按钮系
| 组件 | 现状 | 备注 |
|---|---|---|
| 大按钮六色系：主(金)/确认(绿)/取消(蓝)/危险(红)/特殊(紫)/置灰(灰) | 金蓝已贴图(r4)，绿红紫灰 CSS | border-image 管线已就绪 |
| 小圆钮族：✕ ‹ 锁 ▶ ? 🏠 ⚙ | emoji/CSS | 一张小圆钮表 |
| 购买/充值/领取 | CSS 金系复用 | 并入六色系 |

### 1.3 顶栏与进度
| 组件 | 现状 |
|---|---|
| 头像框 / 等级角标 / 经验条 | 头像框 📦 在库未接：接法已定（框件走 border-image **无 `fill`** 变体，避免盖掉行图标的 background，见 STYLE-SPEC §9 表），等目检框厚；等级角标/经验条 ✗ 预留 `ui/lvtag` / `ui/progress/bar_*` |
| 资源胶囊 金/钻/体力 + 加号钮 | 已贴图(r3) |
| 邮箱 / 公告 / 设置 小图标钮 | 邮箱/设置已贴图(r4)，公告 📣 待补 |
| 体力条 / 经验条 / 关卡进度条（绿/黄/蓝 + 底槽 + 宝箱节点） | CSS |

### 1.4 导航与列表
| 组件 | 现状 |
|---|---|
| 页签 ×5（常态 + 选中态） | 已贴图(r3) |
| 行卡 / 标签 chip / 红点 / 倒计时牌 | CSS（chip 可后置） |
| 品质边框 ×4（白绿蓝紫） | ✅ 本轮接 `ui/frame/frame_q0~q3` → `.popQ .qi`（唯一真源，CSS 白边留作缺图回退） |
| 段位徽章 ×7（青铜→王者） | 📦 2026-09-21 移出包 → `art-spec/reference/stock/rank/`：当前没有段位 UI（D4），在库无宿主就不该占包；排行榜前三另族 = `ui/badge/medal1~3` 预留 |
| 星星 0~3 / 评价星 | × emoji → 预留 `ui/star_on` / `ui/star_off` |

### 1.5 图标族
| 组件 | 现状 |
|---|---|
| 功能图标：邮箱/设置/任务/排行/成就/好友/奖杯/日历/签到/礼盒/喇叭/商店 | 邮箱/设置/任务/排行/成就已贴图(r4)，其余 × |
| 状态图标：盾/剑/心/骷髅/火/冰/电/毒/锁/放大镜 | × |
| 资源图标：金币/钻石/体力（+ 扩展资源预留） | 已贴图(r3) |
| 技能图标 ×12（3 英雄 × 普攻/技能/大招） | 一轮版，清单标「待换肤」 |

### 1.6 内容与战斗
| 组件 | 现状 |
|---|---|
| 英雄立绘 ×4 / 指挥官 / 特遣队 | 一轮版 → 同表重生成 |
| 怪物 ×5 单图 + 12 帧走帧 ×4 | 视频抽帧，风格混源（决策：保留走帧，单图重生成） |
| 战斗场景：路面 / 车尾（主角车，受损两态）/ 护送页图 / 主题 ×4 | 一轮版 |
| 战斗件：飘字 3 色 / boss 血条 / 技能槽 / 波次牌 | CSS/代码绘制 |
| 特效：枪口焰 / 爆爆 / 迫击炮 16 格 / 金币爆 / 升级光 | 一轮版 / × |

## 2. 生成批次表（按 UI 分类，同类一表）

| 批次 | 类别 | 内容（槽位名进 prompt） | 对应组件 | 状态 |
|---|---|---|---|---|
| 批0 | 基准图2 直取 | edit 模式出**绿幕底版**基准图2 → 切头像框×3、品质边框×4、段位徽章×7 | 1.4 框徽 | 可直接开工 |
| 批1 | 面板类 | 九宫格底板 ×2 档（S/M 与 L/XL）、标题绶带、道具详情/奖励/任务面板外框（去字版） | 1.1 | 待 `--rect` |
| 批2 | 按钮类 | 大按钮六色系（去字底板）、小圆钮族 8 件 | 1.2 | 部分 ✅ |
| 批3 | 图标类·功能 | 主城功能入口图标（两批 20 键 → 16 件已落盘接线）；**好友/日历/商店帐篷经第二批核实无槽位已撤**（D18）；二级页签 `ui/ico/tab_*` r13 已出表，**5 件落盘接商城+背包两套页签、`tab_core` 因宿主不存在留单**；余 8 件等宿主决策 | 1.5 | 主体 ✅ |
| 批4 | 图标类·状态+战斗 | 盾/剑/心/骷髅/火/冰/电/毒 + 飘字底、技能槽 | 1.5/1.6 | 未开始 |
| 批5 | 资源+进度 | 加号钮、进度条三色件（槽/填充/端头）、宝箱节点、扩展资源预留 | 1.3 | 未开始 |
| 批6 | 角色类 | 英雄 ×4（2×2 大格同表）+ 指挥官 | 1.6 | 未开始 |
| 批7 | 怪物类 | 5 怪整表（走帧保留） | 1.6 | 未开始 |
| 批8 | 场景类 | 路面/车尾两态/护送图/主题 ×4（单张直出不切） | 1.6 | 未开始 |
| 批9 | 特效类 | 枪口焰/爆爆/金币爆/升级光 | 1.6 | 未开始 |
| 批10 | 技能图标 | 12 技 4×3 整表换肤 | 1.5 | 未开始 |

> 已完成的 r3/r4 产出（nav×5 / res×3 / ico×5 / shop×4 / 按钮板金蓝 / 关闭钮等）视为
> 批2/批3 的已交付部分，后续按新表整族替换。
>
> **每批具体出哪些 key 不在本文重复维护**：槽位清单 = `AssetLib.RESERVED_SLOTS`（预留/采购单）
> + `ASSET-MANIFEST.md` §B/§C（分类导航），通用件的切片参数与态策略见 `STYLE-SPEC.md` §9/§10。

### 2.1 批次审图台账（不合格格子的补出循环）

一表很少 20/20 全合格。**不合格 ≠ 整表作废**：合格的先切片落盘，不合格的 key 原地留在
`RESERVED_SLOTS`（不预载、上屏走 emoji 回退，所以"还没出好"不破任何断言），攒够几个再单独出一张小表重出。
**某类全部 key 落盘 = 该类进版完成**，判据就是这张台账清零，不靠感觉。流程细则见 `STYLE-SPEC.md` §8 流程第 5 步。

| 批次/表 | 槽位 key | 判定 | 依据与下一步 |
|---|---|---|---|
| 五轮 AI 版 `gen-output/r7_icons_*.png` | （整表） | ✗ 整表作废 | 提示词带形象（「图鉴卷轴」「试炼高塔」）违反共性纪律第 1 条；背景草绿抠不净 |
| 五轮 用户版 `gen-output/icons.png` | （整表） | ⛔ 作废 | 背景深灰（提示词只写「纯色背景方便抠图」）→ 切片器认不出整表。逐格判定由下面 r8 接续，不再重复维护 |
| 六轮 r8 `gen-output/r8_icons_*.png` | （整表） | ✅ 可切 | 按新写法出：只报功能名 + 底色点名 `#00FF00`。四角实测 (11,248,18)，默认 tol=60 背景抠除 68.8%、去绿染仅 0.68% → 切片链路通了 |
| 同上 | `ui/ico/ico_add` | ✅ 修好 | 槽位名从「获取资源」改成「加号」即出单个加号——**功能名要贴槽位真实用途**，不是越抽象越好 |
| 同上 | `ui/ico/ico_trial` `ui/ico/ico_endless` | ✗ 不合格 | 两格都出成骷髅头（试炼=环中骷髅、无尽=盾徽骷髅+∞），相邻两键语义撞车、缩到 56px 分不开 → 进补出表，槽位名改得更互斥（如「关卡试炼」「无尽生存」） |
| 同上 | `ui/ico/ico_trial` `ui/ico/ico_starup` `ui/ico/ico_talent` `ui/ico/ico_mute` `ui/ico/ico_info` `ui/ico/ico_warn` | ✗ 不合格 | 6 格套了金属圆环/盾形装饰框 → **用户判：不要框**（边界见 §3「图标一律无装饰外框」：语义形状保留）→ 进 r9 补出表 |
| 同上 | `ui/ico/ico_add`（绿加号） | ✗ 不合格 | 加号本体是绿的、底也是绿的，会走「去绿染」被改色 → 补出时点名「加号用金色」 |
| 同上 | `ui/ico/ico_info` | ✅ 豁免 | 圆牌带字母 i → **用户判：不算违规**，已写进 §6 第 6 条例外 |
| 六轮 r9 `gen-output/r9_icons_*.png` | 8 格补出 | ✅ 合格 | 加号改金色、试炼=石砌地堡、无尽=破土僵尸手（两格不再撞成骷髅）、六格装饰框全部去掉 |
| 图标第一批落盘 | 12 件 | ✅ 已接线 | `ico_add/signin/trial/endless/core/weapon/starup/talent/recruit/forge/del/warn` 切片进 `textures/ui/` 并接宿主；预留声明同步从 `RESERVED_SLOTS` 移出（2026-09-21 分类迁移后该族落盘位是 `textures/ui/ico/`，重切时按新 key） |
| 图标第一批落盘 | 8 件 | ⛔ 切了又删 | `ico_codex/loot/inbox/empty/info/sound/mute/search` 无真宿主（占位在当代在库件手里 / emoji 嵌在长文案里 / 默认分支无人走 / 全工程无检索位），留源表在 `gen-output/`，接线那轮重切。逐条理由见 STYLE-SPEC §9「功能图标·待接」 |
| 七轮 r10 `gen-output/r10_icons_*.png` | 整表 10 格 | ✅ 可切 | 好友/日历/商店/撤销/暂停/伤害统计/滑杆 7 格一次过：按 §8 新写法出（只报功能名 + `#00FF00` 底 + 「不要装饰外框」一句），第一批那 6 格套框的问题整族消失。背景 (24,247,34)、默认 tol=60 抠除 85.67%、主体带绿 0.466% |
| 同上 | `ui/ico/ico_ad` | ✗ 不合格 | 出成**军喇叭**，与在库 `ui/ico/ico_notice`（公告喇叭）同屏撞形 → 槽位名从「广告」改成「播放（视频播放按钮）」，并点名「不要喇叭、扩音器或扬声器」 |
| 同上 | `ui/ico/ico_check` | ✗ 抠不净 | 对勾本体是绿色系（均值 (86,138,50)，条身亮部通道差压过 tol=60），去绿染把条身洗出白斑 → 槽位名补「用金色石质、不要绿色」（同 `ico_add` 加号改金色的老路子） |
| 同上 | `ui/ico/ico_more` | ⛔ 撤键 | 三点/尖括号是随文变色、随文基线的小状态符，128px 位图缩到 14px 只会更糊 → 见 D18 ① |
| 同上 | `ui/ico/ico_calendar` `ui/ico/ico_shop` | ⛔ 撤键 | 同位重复：日历与 `ico_signin` 是侧栏同一个签到键，商店帐篷与底部页签的 `nav_mall` 是同一个入口 → 见 D18 ② |
| 同上 | `ui/ico/ico_friend` `ui/ico/ico_undo` | ⛔ 切了又删 | 没有该上图的功能位：全工程无好友入口；`↩`/`←` 是弹层二级页的**返回**键、语义不是撤销。两个 key 留在采购单等功能，不等美术 → 见 D18 ③ |
| 八轮 r11 `gen-output/r11_icons_*.png` | 2 格补出 | ✅ 合格 | 播放三角＝石质（与暂停两石板同族）、对勾＝金色石质；两格都无装饰框、无喇叭，默认 tol 直接抠净（切片目检见 `gen-output/r11_audit.png`） |
| 图标第二批落盘 | 4 件 | ✅ 已接线 | `ico_ad/slider/pause/stats` 切片进 `textures/ui/ico/` 并接宿主，预留声明同步移出。HUD 两键的图挂 `.hudBtn` **内层 `.ic`**（板面是 CSS 渐变，图直接压按钮会连底一起换掉），为此给 `.hudBtn` 补 `display:flex` 与 `.hudBtn .ic` 尺寸档 |
| 拆行接线轮 | `ui/ico/ico_sound` `ico_mute` `ico_info` | ✅ 重切落盘 | 三件是第一批切过又删的（当时判「emoji 嵌在长文案里、`dropGlyph` 清不掉」）。这次**按码位回到 r8/r9 源表重切**（`--root ../gen-output/scratch_r8`，只取需要的格、不覆盖已定档件），棋盘底目检三件均无抠穿、无绿边；`ico_info` 的蓝圆牌 `i` 属 §3 已豁免的语义形状，不是装饰框 |
| 同上 | `ui/ico/ico_sound` `ico_mute` | ✅ 已接线 | 主城设置弹窗音效行 `_popAttr`（`.ai` 34px）。新增 `PopAttrOpts.iconTex`：glyph 仍写 `🔊`/`🔇` 当占位，图到位由 `icon()` 摘掉——这是「状态换图」（静音↔开启两 key），与 §9 禁的「状态显隐」不同。`_popSec('🔊 音效')` 那处 11.5px 不拆，见 §9 ④ |
| 同上 | `ui/ico/ico_info` | ✅ 已接线 | 战斗页设置浮窗三个小节头 `mkHead(icon, text, tex?)`（`.bSetHead` 30px，改 flex 让图标与标题同行）：音效上 `ico_sound`、关于上 `ico_info`、危险操作复用第一批的 `ico_warn`。HUD 侧统一走 `_iconIc(el, glyph, key?)`，沿用本文件既有的 `_assetBgUrl` 一次性回退口径 |
| 同上 | `ui/ico/ico_codex` `ico_loot` | ⛔ 撤键 | 用户拍板「图鉴和补给箱不用换」→ 位置继续由当代在库件 `shop_scroll` / `chest` / `shop_chest` 服役，两个 key 从 MANIFEST 与采购单撤掉（§9 ④） |
| 同上 | `ui/ico/ico_inbox` | ⛔ 撤键 | 拆行时找宿主才发现：邮箱入口已由第一批的 `ico_mail` 上图，剩下的 `📬 邮箱` 是弹层横幅里的随文小符——同位重复（§9 ②） |
| 同上 | `ui/ico/ico_empty` | ⏸ 仍不接 | 拆行轮只处理了 sound/mute/info 三件；`_popEmpty` 的 14 个调用点仍全部显式传图，默认 📭 分支无人走，挂上去也是假宿主 |
| 九轮 r12 `gen-output/r12_bars_*.png` | 进度条一族 5 格（底槽 + 绿/黄/蓝/红填充） | ✅ 整表合格 | 1列5行、五件宽度一致；底槽＝内凹暗槽带圆头，四件填充＝纯色带石/金属质感、无高光边无圆头。绿件是军绿（g-r 远小于 60），默认 tol 不抠、去绿染也没洗出白斑——**切片件目检过，不是只看源表** |
| 同上 | `ui/progress/bar_track` | ⚠ 定档作废重定 | 照基准图比例定的 `8 fill` / 2px 对不上这张件：`tools/measure_nine.py` 量出左右圆头各 17px、上下描边含软边各 10px，按 8 切会把圆头划进中段拉长成直线。改成四边分设 `10 17 10 17 fill` / `4px 7px × --pu`。**教训：切片档量真正要用的切片件，不是量参考图比例** |
| 同上 | `ui/progress/bar_cap` `bar_node` | ⛔ 撤键 | 底槽件自带圆头端点，不需要独立 cap；节点另有 `ui/node_done/next/lock` 三件在用 |
| 同上 | `ui/progress/bar_fill_yellow` `bar_fill_blue` | ⛔ 切了又删 | 图合格但没有可贴的宿主：黄件等车尾条的橙色三态配套（`.vehicleFill` 的金/橙/红由 `.warn/.danger` 类切换，挂内联贴图会把三态吃掉），蓝件全工程没有冷却/科技类条。源表留在 `gen-output/` |
| 进度条落盘 | 3 件（track / fill_green / fill_red） | ✅ 已接线 | 宿主按实测过筛：**只有战斗 HUD 四条 ≥10px**（`.xpBar`25 `.vehTrack`20 `.bossTrack`28 `.statBar`20）。底槽四条全贴；填充只贴 `.xpFill`(绿) 与 `.bossFill`(红)，`.vehTrack`/`.statBar` 的填充色归 CSS 三态与英雄身份色管、不上图。HUD 层补 `--pu: var(--s,1)`（同 D10b 口径）。主城四条 3~8px 不接、六条是死样式、两条名不符——§9 原先粗写的「主城 11 条 + HUD 5 条」已按实测改写 |
| 进度条拍板轮 | `ui/progress/bar_fill_green` → `bar_fill_blue` | 🔁 换件 | 用户判「经验条改用蓝色」：原 CSS 就是青色 `#4dd0e9`，换绿等于改配色。**绿件删出库、回采购单**（它唯一的宿主就是经验条，换蓝后它没位置了），蓝件落盘接 `.xpFill`。族内四色是出图侧的语义，落到具体宿主要让位于「换图不换观感」——这条写进 §9 填充档 |
| 同上 | 载具条填充 `.vehicleFill` | ⛔ 判死不贴 | 用户判「不换成贴图」：金/橙/红三态由 `.warn/.danger` 类切换，挂内联贴图会把告警色吃掉；要贴就得再出一张橙色件。黄件继续留在采购单 |
| 同上 | 主城四条细进度条 | 📋 排产 | 用户判「单开一轮加粗到能贴图」：`.expbar` 3px / `.rcBar` 6px / `.pbar` 7px / `.popAct .bar` 8px 抬到 ≥10px 再接底槽与填充。**属布局改动**，会挤动主城与弹层行高，单独一轮做，不与美术轮混 |
| 主城加粗轮 | `.expbar` `.offer-copy .rcBar` `.popRow .pbar` `.popAct .bar` | ✅ 抬到 12px 并接线 | 两层 CSS（`--hs` 基准层 / `--pw` 青瓷层）各改同一个 `height`，宽度与圆角不动。底槽走新加的 `barThin` 档（切片同 `bar`、显示宽度按 12÷52 折算成 2px/4px，见 D21），填充按原 CSS 色归语义：顶栏经验与抽卡保底＝蓝、弹层行进度＝绿、弹层活跃/保底＝黄（两处调用点）。**绿/黄两件因此落盘，进度条一族五件整套出采购单** |
| 同上 | 主城顶栏 `.expbar` 抬高的版面风险 | ✅ 无头实测过 | 540×960 手机口径截图核对：条从 3px 变 12px 后，`#homeUi .xpRow` 里「末日指挥官」+ 条 + 「波次 0 · 通关 0/5 章」仍一行不挤，资源胶囊与右侧邮箱/喇叭/设置键无位移。顺带证伪了一个担心：`.expbar` 写着 `border: none`，九宫格照样画得出来（`border-image-width` 自带厚度，不依赖 `border-width`） |
| 加粗轮真机回调 | `#homeUi .expbar` 12px → **10px** | ✅ 已改 | 用户看过手测点后判 12px 偏粗。10px 是 `barThin` 档下还能留得出内腔的最低线（板边 2px 上下、内腔 6px），再薄就该退回不贴图 |
| 加粗轮真机回调 | `#homeUi .offer-copy .rcBar` 12px → **6px 且不贴** | ✅ 已撤 | 用户拍板三选一里的第三项：不加粗、不贴图。12px 那版会把商城主推卡顶高约 6px、可能让下面双列商品多出一行滚动；6px 也配不出内腔。撤掉 `HomeUiMall` 的 `_barTex` 调用，蓝件仍有 `.xpBar` 与 `.expbar` 两个宿主 |
| 十轮 r13 `gen-output/r13_tabs_*.png` | 二级页签 6 格（英雄/装备/宝石/材料/核心/道具） | ✅ 整表合格 | 3列2行一次成表，行主序与槽位表逐格对上；棋盘底目检 6/6 无抠穿、无绿边、无邻居碎件并入。人物半身（英雄）、步枪（装备）、蓝钻（宝石）、铝锭+木料+石（材料）、反应堆芯（核心）、医疗包（道具）——**六格都不带装饰外框**，页签底板仍归 CSS，符合 §5「页签图标单主体不带托」 |
| 同上 | `ui/ico/tab_hero` `tab_equip` `tab_gem` `tab_mat` `tab_potion` | ✅ 五件落盘接线 | 两套页签各建 `span.ticon` 图标位（同底部主导航 `.tab .ticon` 的做法：glyph 占位、贴图到位摘字、尺寸归两层 CSS）。商城与背包的装备/宝石/材料三签共用同三件，所以 5 件吃下 7 个位置。背包第四签真名是「道具」，键沿用 `tab_potion`（医疗包读得出耗材），映射写进 §9 免得下轮按 key 名去找「核心页签」 |
| 同上 | `ui/ico/tab_core` | ⏸ 图合格但无宿主 | 规范里那第三个宿主 `.chTabs`（章节页签）**不存在**：样式还留在 `HomeUiStyle.ts:758`，DOM 早在护送页改版时换成「章节头 + 场景内侧左右翻页箭头」（`HomeUiStage.ts:68` 注释即「替代原五章页签」）。背包也没有核心分类，武器核心只是养成弹窗里的一行。**键留在采购单、切片件不落盘**（落盘就会破「在库件必须有归宿」那条断言）。同轮拍板「核心页签功能我后面做」，图已从 `gen-output/`（gitignore、定期可清）挪进 `art-spec/reference/stock/ico/tab_core.png` 长期存着，界面建好拷回即生效 |
| 同上 | 二级页签图标尺寸 | ⚠ 规范值作废重定 | §9 原先粗写「与 `.hot .ic` 同口径 20~34px」，实测两套页签都并进了 `.flat-tabs` 条带（34 高、文字 12 号），34px 图标会把行撑高；定 20px ×层缩放。**抄别的槽位的尺寸等于没量**——同 D19 的教训，只是这次量错的是宿主尺寸而不是切片尺寸 |
| 十一轮 r14 `gen-output/r14_medals_*.png` | 名次奖牌 3 格（金/银/铜） | ✅ 整表合格 | 3列1行；三件同款只换金属色，骷髅＋双翼＋铆钉环＋底部尖石，描边一致。棋盘底目检 3/3 无抠穿、无绿边。**环心是镂空的**——这不是瑕疵，出图时就写了「正中留出空位，那个位置要叠名次数字」，模型照做了；切片件在棋盘上看着像缺了一块，所以这条判据必须连着台账一起读 |
| 同上 | `ui/badge/medal1` `medal2` `medal3` | ✅ 三件落盘接线 | 宿主只有一处：战斗 HUD 伤害统计 `.statRank.rank1/2/3`（建行 `DomHud.ts:1249`、每轮重排名 `:881`）。走 `icon({keepGlyph, hideBorder, color})`：数字留在环心、边框隐掉、墨色换成同档金属色（三个 UiTheme 令牌，不写 hex），字号 34→20×--s 才塞得进环；第 4 名不贴图不改色。**去重靠 `dataset.medal`**——`_refreshStats` 会反复刷排名，不去重每次都往挂起队列塞一条 |
| 同上 | 排行榜弹窗那格 `.popRow .tag` | ⛔ 判死不贴 | 实测 10.5px×--hs / 26px×--pw，是塞在胶囊里的随文小符号，128px 位图缩到那儿只会更糊（§9「不该出图」判据①）。它原本是 §9 给奖牌写的第二个宿主，已按实测划掉 |
| 同上 | `.lbRank .medal` | ⚠ 死样式（新发现） | 规范写的第三个宿主。查下来整块排行榜 CSS（`HomeUiStyle.ts:548~565`、`1880~1893`，含 `lbEntry/lbBox/lbMy/lbList/lbRow/lbRank/lbIc/lbName/lbScore`）全工程没有一处建 DOM——live 排行榜走的是通用 `_popRow`。**本轮只划掉宿主，那 20 行孤儿 CSS 不清**（属另一轮的死样式清扫，见 §6 下一步） |
| 十二轮 r15~r23（批量出图尝试） | 剩余采购单 43 件，按 UI 分类一次出 10 张表 | ⛔ **中断：生图接口余额用尽** | 已出 8 张（`gen-output/r15_btn` `r16_panel` `r17_tag` `r17b_star` `r18_res` `r19_mat` `r20_veh` `r21_status`，共 40 格）；`r22_battleui`（波次牌/技能槽/boss 徽）与 `r23_ico`（空态/好友/撤销/搜索）返回 `403 billing_error: insufficient balance`，未出。**这 8 张表只在 `gen-output/`（gitignore、定期可清），补不到就重出** |
| 同上 | 出图前逐族核宿主 | 📋 结论进 §9 | 43 件里 **31 件根本没有宿主**：能直接进版 10 件、要先改 DOM 才能贴 5 件、功能不存在 10 件、界面没入口 8 件、同位重复或位置是死的 8 件。逐条判决见 STYLE-SPEC §9「采购单宿主全量核实」。**「一次全出完」不慢在生成，慢在那 26 件对应的界面还没做** |
| 同上 | 提示词「正中留出空位」的歧义 | ⚠ 措辞要改 | r17 那五件角标被模型做成了**镂空圆环**（「留空位」读成了「挖洞」），角标是要放「免费/HOT」这类文字的平整牌面，整表作废重出。r14 奖牌那次镂空是想要的（数字小、环大），这次不是。**下次写「正中是一整块平整空白的牌面，不要镂空、不要挖洞」** |
| 十三轮 r19 `gen-output/r19_mat_*.png` | 材料与宝石 7 格（石料/合金/核心 + 火/风/冰/雷宝石） | ✅ 七件落盘接线 | 4列2行表，模型把第 8 格也画满了（琥珀雷宝石）——不在采购单上，丢弃。**默认 `--tol 60` 会把绿宝石抠穿**：它的亮绿漩涡满足「g-r>60 且 g-b>60」，被当背景键掉了；棋盘底目检发现，按 §8 的既有结论调大到 95 重切，七件一次过。宿主是背包格 `.bcell i` 与商城货卡 `.gIc`，两处原来只塞 emoji、没有贴图槽，本轮各加一个（`mkGood` 新增 `icTex`；背包格把 innerHTML 拼串拆成元素才挂得上图） |
| 同上 | 材料/宝石的 key 映射 | 📋 立成契约表 | 新增 `UiPlate.MISC_TEX`（id → 整串 key）。两条理由：① `'icons/'+id` 拼出来的串 `check-art-manifest` 不认，会把已落盘的图判成「在库无归宿」；② 表里只列已出图的 id，`mat_blueprint` 查不到就走 emoji——贴图挂起队列只排得空已登记 key，把没图的 id 塞进去会留下一条每 400ms 自续重试的空转定时器 |
| 十四轮 界面表面普查（`tools/audit_ui_surfaces.mjs` + `tools/page_shot.mjs`） | 主城五页 + 战斗 HUD 每一面的实况尺寸与「有没有贴图」 | 📋 结论进 §9 | 用户点名「美术进版很乱、界面完全不行」，先不猜原因：把六张界面逐面拉一份清单（类名 / 文案 / 盒子 / 圆角 / 边框 / 有没有 borderImage / 有没有渐变）。**普查直接给出根因**：`.game-button` 这一族最大的 CTA（开始护送 307×72、十连 203×47、立即查看、单抽）从来没有底板，全工程只有 1 个手工铺板调用点。清单同时把「采购单里写了宿主其实不存在」那批坐实到具体像素（`.game-button.sm` 46×22、`.squadEntry` 40×39、`.btn.blue.hot` 61×61）——**没有这份实测，按钮族只能照旧猜尺寸** |
| 十五轮 r24 `gen-output/r24_buttons_*.png` | 主城按钮族 8 块板 | ✅ 4 件落盘接线 | 4列2行表：小圆钮 主页/帮助/刷新 + 小键 `btn_small`；特殊(紫)板 `btn_purple` + 侧栏入口 `btn_side` + 列表行 `btn_row` + 去字主键。**「正中是一整块平整空白的牌面，不要镂空、不要挖洞」这句改写生效**——r17 那次挖洞的坑这次 8/8 全部平整。`btn_row`/`btn_side` 是横长件，按 `:WxH` 目标比例出图（fit 不拉伸）。三件小圆钮仍留采购单：主页/刷新这两个动作在界面上还没有落点，「帮助 ?」同位已由 `btn_round2` 接在弹层角上，属同位重复 |
| 同上 | 按钮族接线方式 | 📋 立成契约表 | 新增 `UiPlate.CITY_BUTTON_PLATE`（选择器 → key + 切片档，顺序即优先级，`key: null` = 显式跳过），`HomeUiCore._plateCityButtons()` 在 `_switchPage` 尾部整族扫，扫描用的选择器由表自己拼出来——**加一档不用再改代码**。切片新增 `btn`（12px 显示宽）/`btnSm`（7px）两档，切法同为 `24 fill`（D21：切片不变、显示宽度随宿主厚度走）。派生态不出二态图，改用同族两条选择器指到不同语义板（`.diffSeg.on` 金 / `.diffSeg` 蓝；`.squadEntry.on` → `btn_confirm`）。**三处实测跳过**：46×22 难度小键（板比键还厚）、61×61 英雄养成五入（板只盖住中段一条，图标与文案露在板外，要先改 CSS）、章节翻页 `.arrow`（板融进深色实景照片）。接线后自测：商店 0→9、英雄 0→17、护送 0→20 面有板 |
| 十六轮 r21 `gen-output/r21_status_*.png` | 状态与属性图标 10 格 | ✅ 8 件落盘接线 | 5列2行表，目检 10/10 无抠穿、无绿边（这批没有绿色主体，默认 `--tol 60` 就够，不用像 r19 绿宝石那样调到 95）。**一张表吃下两套词缀**：精英词缀（迅捷/坚甲/治疗/分裂/狂暴）与装备词缀（狂暴/精准/迅捷/鹰眼/穿甲/铁壁）语义高度重叠，统一进 `UiPlate.STATUS_TEX` 一张 id→key 表（两边 id 不撞，后者带 `af_` 前缀）。⚠ **「锁定」那格模型画成了分划板而不是挂锁**——将错就错：它正好对上装备词缀「精准」（🎯），而本作真正的「锁」是跟在文案里的 🔒 随文小符号（按 §9 判据①本就不出图）。⚠ `status_ice`/`status_poison` 两件**不落盘**：图合格，但战斗里没有冰冻/中毒这类持续伤害机制，挂上去就是一颗永远不亮灯的死槽，切片件与 `tab_core` 同样归档 `art-spec/reference/stock/ico/`、键留采购单 |
| 同上 | 状态图标的两个宿主（原来一个都没有） | ✅ 新建 | ① 战斗 HUD 波次 chip 底下加一行 `.afRow`：场上存活精英身上出现过的词缀各摆一枚徽标，0.4s 刷一次、怪死光整行收起。为此给 `BattleManager` 开一个 `get liveEnemies()`（只读，DOM 层不许改）——原来精英只有头顶那枚画布徽，说的是「这一只」带什么词缀，玩家看不到「这一波上有 什么哪些」。② 装备详情与重铸弹窗的词缀行 `_popAttr` 补 `iconTex`（两处调用点各一行）——`_popAttr` 本来就有贴图槽，只是从来没人往里传 |
| 十七轮 r23 `gen-output/r23_ico_*.png` | 功能图标补件 4 格（空态/好友/撤销/搜索） | ✅ 3 件落盘接线 | 4列1行表，目检 4/4 合格。**这一轮按用户新令改口径**：上一轮对 friend/undo/search 的判决是「全工程没有功能位，键留采购单、图不落盘」，本轮改成「**没有落点就把落点建出来**」。① `ico_search` → 背包头行新增按名字检索框（四个页签共用；整页重建会把焦点丢掉，所以回填后要把光标放回新输入框末尾）；② `ico_friend` → 行动页页脚第四快捷（社交系统还没做，但弹窗有说明 + 「去看排行榜」的出路，不是静默死键）；③ `ico_empty` → 改成 `_popEmpty` 的**默认件**，并把「背包中该部位没有其他件」一条调用点改为不传 icon（原来 15 处全部显式传图，默认分支没人走，落图也是假宿主）。④ `ico_undo` **仍不落盘**：全工程没有一个可撤销的动作，凭空造一个点下去无事发生的「撤销」键破的是「无死键」红线——留给下一个真正可撤销的操作（首选天赋误点后的「撤销上一次加点」） |
| 同上 | 默认空态件的缺图空槽 | ⚠ 顺手修 | `_popEmpty` 原来传贴图 key 时把 glyph 直接置空，预载晚于建 DOM 那一刻就是一个空槽；现改成先摆占位字形、图到位由 `icon()` 摘掉（与 `_popAttr.iconTex` 同口径）。📭 在源码里是代理对、按字面量匹配不上，故提为 `EMPTY_SLOT` / `EMPTY_GLYPH` 两个模块常量 |
| 同上 | **自测抓到的真缺陷：铺板只挂在 `_switchPage` 上** | ✅ 修 | 取景对比同一页两次截图发现：在背包检索框里打一个字（走 `_refreshHeroes` 就地重绘，不走 `_switchPage`），那一页所有按钮板全部消失——`_plateCityButtons` 写的是内联 style，而重建把这些元素整个换掉了。`_applyPendingTex()` 不是通用收尾（`HomeUiPlay` 一次都没调），逐处补一行迟早漏一处。改成 `HomeUiMall._watchCityPlates()`：`MutationObserver` 盯 `#homeUi` 的**子树增删**，有重建就在下一帧重扫一次。两条口径同时收紧：① 只观察 `childList`，铺板写的是 style，属性变更不进名单所以不会自激；② 扫描范围从整个 `#homeUi` 收到 `.viewport`，弹层 CTA 仍归 `_openPop` 的 kind 路由，两套规则不再互相覆盖 |
| 同上 | `ui/ico/ico_skill` 撤键 | 📋 记档 | 技能这一族按用户明令「除技能外不换」保留 emoji，那这个键永远不会有人填。留在 MANIFEST 里就是一条永远缺文件的空槽（预载白发请求、对账永久挂账），`MANIFEST` 与 `RESERVED_SLOTS` 双双删除 |
| 十八轮 r17b `gen-output/r17b_star_*.png` | 评价星 2 格 + 天赋节点 3 格 | ✅ 5 件落盘接线 | 这一张表是十二轮批量出图时留下的 8 张里的一张（当时因余额中断没处理到）。**切片要 `--dilate 2`**：默认膨胀下星尖与节点环的细边会被邻格绿底渗进来，目检棋盘底一圈发暗；改 dilate 后 5/5 干净（`gen-output/cs_r17b.png`）。星两态（亮金 / 暗金属）与节点三态（可点蓝 / 点满绿 / 未解锁灰锁）都是同一形状换材质，符合 §5「族内只换色不换形」 |
| 同上 | 评价星的宿主 | ✅ 已接线 | 原来图鉴详情里那一排「威胁等级」是 `'★'.repeat(n) + '☆'.repeat(5-n)` 一条**纯文本**——文本星根本没有元素可以贴图。新建 `HomeUiMall._starRow(n, max)`：建 5 个 `<i>`，每个各挂一张星图（亮/暗两 key），尺寸归 `.starRow i` 单层 CSS（`calc(26px*var(--pu,1))`，这一族只有深色层一种版面，不像 `.uiSearch` 要两层各写一遍）。**教训：采购单写「宿主 = 星级文本」的槽位，先要把它从字符串改成元素，才谈得上贴图** |
| 同上 | 天赋节点的宿主 | ✅ 已接线 | 天赋图 `_popGrid` 原来把状态画在 CSS（`.popGrid` 格的边框/底色区分未解锁/可点/点满），没有贴图位。给 `_popGrid` 的 items 加一个可选 `tex` 槽（`HomeUiCore` 那一行 `if (it.tex) … icon(cell, { keepGlyph: true })`——Core 只剩 1 行余量，所以是"加槽"不是"新建组件"），三态各指一个 key。emoji 留着当占位，图到位不摘（节点环是**框**，中间本来就要放技能图标） |
| 同上 | 升星弹窗那排星 | ⏸ 本轮不接 → ✅ Step4 解开一半 | 同一个 `_starRow` 的第二处自然宿主是英雄页的星级。**不是没做，是 `HomeUiHeroes.ts` 已经 2490/2490 顶到行数预算**，再加一行就 FAIL；`check-split.js` 的红线是不许把预算改大，所以先接线一处、把拆文件排在接第二处之前。**下一轮真解开了**：卡面副标那处（`def.role · ★★`）改由 Mall 层的 `_starInline` 建，调用点只是**把一行换成另一行**、棘轮一格没加——「顶满」挡的是加行，不挡改行。仍没接的是升星弹窗的 subtitle（那是传给 `_openPop` 的字符串，要开 Core 的槽，而 Core 只剩 1 行余量） |
| 十九轮 r25 `gen-output/r25_tag_*.png` | 角标与徽章 5 格（等级/免费/折扣/HOT/战力） | ⛔ 整表合格但**判形作废** | 出图前按十四轮普查量的尺寸，五格全部按「正中是一整块平整空白的牌面，不要镂空、不要挖洞」出，5/5 平整合格（`gen-output/cs_r25_tag.png`）——十二轮那次挖洞的措辞问题确实修掉了。但把宿主逐个量形才发现：**模型给的是方形/六角/星芒的"徽章"，宿主是扁的"角标"**：`.lvtag` 21~34×13~18px、`.good .gHot` 约 20×12、`.gHot` 60×36 的 99px 圆角胶囊、`.powerBadge` 27px 高的整宽胶囊，`.gTagTop` 干脆是死样式（全工程无建点）。硬贴要么把徽章压成饼、要么盖掉文字。**五件全部不落盘**，切片件归档 `art-spec/reference/stock/badge/`，键留采购单并写实测形状。新规则进 §9：**出图前除了量宿主尺寸，还要写下宿主的轮廓**（扁角标 / 方徽章 / 圆钮 / 胶囊），形不对等于件不对 |
| 同上 | 自测踩到的两个坑 | 📋 修工具 | ① **隐藏 DOM 量出来全是 0**：`#homeUi` 在 `display:none` 下用程序 `.click()` 照样能触发建 DOM、贴图也会挂上（`bg=Y`），但宽度一律读成 0——第一次自测据此报"星没上图"是假失败。改成先真退出战斗（`#domHud .hudBtn.menuBtn` → 退出）再 `waitUntil` `#homeUi` 可见，量到 `w=36`。② 天赋图的入口「前往天赋图」是页脚动作 `.abAct` 不是页签，按页签文案找不到就点错位置。两份坑都在 `tools/smoke_headless.mjs` 里补了取值探针（`talentNodes` / `starRow` 直接打印每个格的 `bg=Y/N` 与盒尺寸） |
| 二十轮 r26 `gen-output/r26_resveh_*.png` | 章节载具 3 件 + 资源·英雄碎片 1 件 | ✅ 四件落盘接线 | 4列1行一次出齐，四件都是单主体、无外框无铭牌无文字，卡车/船/重卡三种剪影一眼分得出。**这张表要 `--dilate 2`**：默认膨胀 6 在 1/4 采样上等于全分辨率 52px 闭运算，而载具之间只剩几十像素间隙——四件直接被并成一件（识别到 1 个、`--expect 4` 当场 FAIL）。改小膨胀一次切开，棋盘底目检 4/4 无抠穿、无绿边、无邻件碎屑 |
| 同上 | 载具牌为什么开在章节头、而不是场景里那块 `.veh` | ✅ 新建落点 | 这一族量的不是尺寸是**可见性**：浅色（手机）主题把 `.stage-scene > .veh` 连同 road/dash/mobs 一起 `display:none`（护送页手机版改用 escort.png 实景照片），那块载具只在深色桌面层活着——贴上去真机上永远看不见。改成在章节头左端开一枚 `.chVeh`（`HomeUiStage` 建元素 + 刷新时按 `CHAPTER_THEMES[].veh` 换字形换 key），它是手机上唯一看得见「本章护送什么车」的位置。key 用**字形对 key**而不是章节序号对 key：章节→载具的映射只有 `CHAPTER_THEMES` 一处真源，抄一份序号表就是两处真源 |
| 同上 | 顶栏资源胶囊为什么不给碎片 | ⛔ 判不贴 | `.reswrap` 硬写三列、`mkRes` 只调 3 次，看着像"加一格就行"，但碎片是**四英雄各自的库存**（`shard_rifle/sniper/laser/radiation`），不是能加总的单一货币——顶栏摆一个碎片总数是骗人。改接升星弹窗那条碎片说明行（`_popAttr` 本来就有 `iconTex` 槽，调用点加一个字段即可）。弹窗底部那枚成本格（`.popCost`）是第二处自然宿主，它由 Core 建、而 Core 只剩 1 行余量，留到拆薄那一轮 |
| 同上 | `res_medal` / `res_energy` / `res_ticket` 三件 | ⛔ **整批不出图** | 本作 `ResourceId` 只有 gold/diamond/stamina + 四英雄 shard_*：勋章没有成就/军团玩法（`ico_achieve` 早已因「主城无成就入口」移出包）、能量与体力是同位重复、招募券的「每日免费一次」是额度不是库存。**用户令「没实现的功能也要把美术用起来」到这一类停手**：凭空挂三个恒为 0 的假数字，破的是「无死键」红线，比缺一组图更伤界面（同 Step2 的 `ico_undo`、`status_ice/poison`）。三行 `RESERVED_SLOTS` 声明都写上了"为什么不排产"，等经济扩表当天直接照行施工 |
| 同上 | 自测：取景停在上一层弹层 | 📋 修工具 | 「护送页载具牌」那张截图拍出来还是图鉴详情——返回钮是圆钮 `.popBack`，取景脚本只点了 `.popClose`。数值仍然量到（`bg=Y、39×39`，说明底下那页确实建好也贴好了），但**那张图不能当视觉证据**：改成两个钮都点，并另用 `tools/page_shot.mjs` 单独取一张干净取景（`gen-output/step4_escort.png`）。**判据**：截图前先确认页面确实切到了目标页 |
| 同上 | 行内星级只有 CSS 能自测 | 📋 如实记 | 新档所有英雄 0 星，卡面上本来就没有星可摆，所以 `_starInline` 只能验 CSS 口径（临时插一个 `.starIn` 量完即删，量到 15×15、字号 15.2px，说明青瓷层的 `--pw` 生效）。数据侧要等有重复英雄的存档才能复验——**这条限制写进台账，不拿「代码接了」冒充「屏上有了」** |
| 二十一轮 r27 `gen-output/r27_battleui_*.png` | 战斗 UI 件 3 格（波次牌底 / 技能槽底托 / 首领徽） | ✅ 三件落盘接线 | 3列1行一次出齐，三件全部平整（`--dilate 2` 同 r26 的坑：横长件之间只剩几十像素间隙，默认膨胀 6 会把整表并成一件，`--expect 3` 当场报识别到 1 件）。**这是「正中是一整块平整空白的牌面，不要镂空、不要挖洞」那句改写的第二次验证**：r22 那张表原话写「正中留出干净的空位」被做成三个镂空洞而整表作废，这次波次牌与底托两格都按新话术出，模型留出的是一块实心牌面 |
| 同上 | 波次牌切片：横长件改用**百分比档** | 📋 新档 `chip` | `measure_nine.py` 量出来「左右圆头 11px」，但那是"柱高爬到满高 98% 所需列数"这个启发式在横长件上的失效读数——牌的四角包边铆钉实际约占 18% 高、12% 宽。照 `card` 那种像素档（24）切会把包边划进中段横向拉长成 smear，故新增 `UiPlate.NINE.chip = slice '18% 12% fill'` / 显示 14px×--pu（按 chip 实测高折算，同 D21）。**推论：`measure_nine` 的圆头读数只对"边是圆头"的件有意义，带装饰包边的件要按包边占比给百分比档**。**自测截图另外判出一条**：这块板起初只贴在波次一枚上，旁边同族的击杀 chip 仍是暗玻璃——两个并排的读数牌一木一玻璃，读起来像"没做完"，现两枚共用同一件（key 名带 `wave` 是当初登记写窄了，映射按 `tab_potion` 那条口径记进 §9） |
| 同上 | 技能槽底托：本轮第一件**画布层**贴图 | ✅ 已接 | 宿主不是 DOM，是 `AbilityBar` 里每个技能图标背后一枚（图标 `ICON_R*2 = 92` 设计像素，托给 112 让四角露出来）。做法跟着该文件既有的「美术就绪即应用一次」探测（内芯图标就是这么办的），只是多一个 `Sprite` 节点：建在图标节点最底层、缺图整节点 `active=false`，冷却遮罩与充能环的半径口径一行没动。**画布件的自测口径与 DOM 不同**：DOM 读 `backgroundImage`，画布只能看截图（`gen-output/smoke_step5/04-battle.png`） |
| 同上 | 首领徽：`.bossName` 的 `👑 名字` 拆成元素 | ✅ 已接 | 与 Step3 那串 `'★'.repeat()` 同一个失效模式——皇冠原来是文本里的一个字，没有元素就贴不上图。现拆成 `<i class="bossCrown">👑</i>` + `<span>` 名字，尺寸归 CSS（30px×--s）。⚠ 这一枚只有 BOSS 波在屏上，取景必须等血条出现才量得到，脚本已改成"看见精英不立刻收工、等 `.bossBar` 亮灯再截一张" |
| 同上 | **差点把图贴到死代码上**：`panel_mini` 结案 | ⛔ 不落盘 + 新判据 | 上一轮给它的判决是「宿主 `.mbox` 带 `frame` 类，板与装饰伪元素打架、口径未定」。本轮按"先接了再说"去量：`.mbox` 全工程只有两个建点（`HomeUiCore` 的 `_openSheet` / `_openResult`），而**这两个方法零调用点**——它们是「全部二级界面已迁新弹层」那条迁移棘轮的桩子（`check-ux-refactor` 断言调用点为 0、`check-ux-layout-bundle` 断言方法还在包里），删不得也用不上；顺带查出 `.mbox/mHead/mClose/mSub/mRow` 整族是那一代遗留的死样式。**最要紧的一条**：我在这两处加过 `nineSlice` 再撤掉，加着的那一轮 `check-art-manifest` 的「在库件必须有归宿」是**绿的**——它只扫代码引用，死方法里的引用照样算数。判据补进 §9：**核宿主先核「谁调用这个建点」，不是「哪里提到这个 key」**。切片件归档 `art-spec/reference/stock/panel/` |
| 同上 | Core 行数还账 | 📋 2239 → 2234 | 接 `panel_mini` 那两行时发现 Core 只剩 1 行余量，按红线（只许拆薄、不许调预算）把 `_popWarn` 从 Core 挪到 Mall：它在 Core 内零调用、19 个调用点全在 Mall 及更下游，挪走照样人人可用。**这一档余量后来因为 `panel_mini` 判死没花掉**，但"往下游挪一个零内调用的组件"是比"删注释"体面得多的还账路子，记下来复用 |
| 二十二轮 r28 `gen-output/r28b_bullet_*.png` | 弹道 2 格（狙击弹 / 辐射弹） | ✅ 两件落盘接线 | 宿主早就在等图：`HeroCombat` 出弹时带 `visualKey`，`Bullet._applyVisualAsset` 取到图就叠在程序化弹体上、取不到就继续用 `Graphics`——所以这两件**零代码改动**。**辐射弹必须 `--tol 95` 切**：它的 `bulletColor` 是 `(178,255,89)`，g-r 只有 77，默认容差 60 会把它当荧光绿背景抠掉（r19 绿宝石同一个坑，判据都是"看棋盘底、不是只看源表"）。⚠ 第一次出图直接失败：生图接口拒绝 4:1（`aspect ratio must not exceed 3:1`），改 2048×1024 重出 |
| 同上 | 丧犬走帧 r29（6 帧，**品红底**） | ✅ 落盘且零代码 | 其余四怪的走帧来自参考视频（`video_to_sheet.py`），仓库里没有狗的视频，所以这次走"图生图定身份"这条路：`--image monsters/dog.png` 当第一张参考（保持是同一只：烂毛、背脊铁刺、獠牙、红眼、脚环），第二张给基准图定风格，prompt 里写清"六帧只有四肢与躯干起伏的差别、视角与参考图一致（正面朝镜头冲来）"——出片 6/6 是同一只，直接过。打包用现成的 `tools/slice_walk_sheet.py`（品红抠底 → 按透明列间隙切帧 → 底对齐统一帧格 → 横条打包），产出 1830×455。**为什么 6 帧不用改代码**：`ANIM_FRAME_COUNT` 只给 boar/bear/eagle/stoneape 显式记了 12，walk 的默认值就是 6；狗正好用默认。**将来重出 12 帧必须补 `'dog:walk': 12`**，否则会把 12 帧当 6 帧切、每格里叠两个半角色 |
| 同上 | 车尾受损态 r30（图生图 + 裁回原比例） | ✅ 落盘 + 新工具 | 这是 D2 早就点名"只有语义真两态才出双件"的那一例（完好 ↔ 被打坏）。以 `scenes/vehicle_tail.jpg` 为参考图做图生图，prompt 里第一句就是"同一台车、同一视角、同一构图与比例，观众要一眼认出是同一台车被打坏了"，并明确"只要已经坏掉、不要正在燃烧"（不要火焰与烟雾）。原图 2160×540 是 4:1、接口最多 3:1，所以先出 2048×1024、再用新工具 `tools/crop_letterbox.py` 按内容外接框裁回 4:1（2048×512）——**两态比例必须一致，否则换图那一瞬车尾会跳一下**。接线在 `Vehicle._syncDamageArt`：耐久 ≤25% 换受损件，门槛与 HUD 车尾条的 `.danger` 取同一个数，让"条变红"和"车被打烂"是同一件事 |
| 同上 | 另 9 件战斗表现键（背景 4 / 特效 4 / 激光束） | ⏸ **整批不排产** | 逐条 grep 过：全工程连拼这些 key 的地方都没有——战斗底图只有 `scenes/road` 一个消费点、结算面是 DOM 弹层、升级走横幅 + 画布施法环、波次衔接是代码淡入淡出、飘字是 Label 直接画字、laser 英雄在 `visualKey` 那个三元里根本没有分支。**这九件缺的不是图，是表现代码**：先出图只会得到九个躺在包里没人引用的文件（正是 D6 要防的「预载但不引用」）。键与"为什么现在出图也没人读"都写进 `RESERVED_SLOTS`；要清账得先建消费点，或者撤键（撤键要用户点头，本轮不动） |
| 二十五轮 r32 `gen-output/r32_20260921_054831.png` | 商城主推卡立绘 1 件（`scenes/recruit`） | ✅ 落盘接线 | 普查把 UI 板族清完之后回头看图本身：商城两张主推卡共用 `.offer-art` 骨架，礼包卡早已换成 `scenes/escort` 实拍照片，招募卡却还是「灰底 + 🎖️ emoji」——同一个骨架两套观感，是普查里剩下最大的一块灰。出图走图生图：第一张参考给 `scenes/escort`（定实拍质感与调色，同族卡面要像同一次拍摄），第二张给主基准。prompt 里两句关键约束是**给布局用的**：「构图重心偏右、左侧留出较暗的呼吸空间（那一侧要叠标题）」、「不要人物正脸、不要任何可读文字」。接线照礼包卡那条 `_tex` 的写法，**emoji 只在照片到位那一刻才摘**（缺图回退时灰底 + emoji 仍是对的）。1024×1536 与 escort 的 2:3 同比例，`cover` 裁切口径一致 |
| 二十六轮 r33（四张单独生成，非整表） | 关卡主题背景 4 件 + 按章换底图的表现代码 | ✅ 建码 + 落盘 + 五关各取景 | 用户点名「把这九件的表现代码建起来」，按可见度从大到小清。**第一件是关卡底图**：`BattleManager._applyRoadArt` 原来硬取 `scenes/road`，改成读 `StageInfo.backdrop`（新增可选字段，第 1 关故意不写、免得与 road 两处真源），取不到回退 road——回退不能省，这方法是逐帧重试到取到图为止的，只认本关 key 就会让图缺失时整局没背景。**键名改了**：旧登记 `bg_forest/beach/snow/cave` 是当初按通用地形词粗写的，跟五关真名只有两张对得上，照旧名出图会有两张永远没人读；这四个键从来没有文件也从来没有引用，改名零代价。出图口径三条：正俯视、道路纵向居中占宽约六成、**上下两端不许有横向通栏大特征**（这张图要上下镜像循环滚动）。四张各自以 `road.jpg` 图生图锁机位与质感，`tools/land_bg.py` 裁到 720×1280 并压 JPG（180~209KB/张，对齐 road 的 266KB 口径；直接进 1024×1792 PNG 会让主包凭空多十几 MB）。**新增自测工具** `tools/battle_probe.mjs`：按关写补丁存档进战斗（`load()` 每字段都是 `?? 默认`，只写 `stageCleared/currentStage/res` 就够，但体力必须补——写补丁档就等于老档），画布件 DOM 量不到，另在 `_applyRoadArt` 补一行 `[Art] 关卡底图生效` 诊断（口径同弹体那行）。五关实测各换上自己的底图、道路几何与车道线都在、HUD 读数牌在新底图上仍可读 |
| 同上 | 自测口径：画布件怎么算"上了屏" | 📋 三条证据各不同 | DOM 件读 `backgroundImage`，画布件读不到，所以这一类每件另找一条能看的判据：① 弹体用 `Bullet.ts` 自带的诊断行「[Art] 弹体正式贴图生效」——取景脚本的 `[page]` 过滤原来只放行 `DBG-AFFIX`，现多放行这一条；② 走帧用**比例**：序列帧格 305×455（竖窄）与静态立绘 352×279（横宽）差得远，而 `_showArt` 按 rect 宽高比给宽，所以一张截图就能分辨狗用的是哪条路径；③ 受损车尾要打到 25% 以下才换、而 GM「车打空」会顺手判负，所以这一件排在整轮最后单独开一局拍（`12-tail-damaged`） |
| 二十三轮（无新出图，纯修档） | 九宫格板族「板面只渲染出盒子高一半」 | ✅ 9 件裁紧 + 4 档改档 | **本轮没有生成任何图**，是修前面几批板件的一个共性缺陷。起因是用户报「弹层按钮的字溢出板面」，但普查把三个数量出来（盒 165×59、内边距 30.46、文字实需 100）→ **横向根本不溢出**，溢出的是竖向：木板只铺满盒高约 55%，字压在板上下的空白里。往源头查是两刀叠在一起：① `tools/slice_sheet.py` 的 `extract()` 把每件**方化**成「最长边×1.06」的正方形画布，横长板件因此上下各垫一大圈 alpha=0（`btn_play` 是 384×384 的方图、真板面只有 364×210，上下各 87px 空边）；② 切片档 `plate = 16 fill` / `btn = 24 fill` 小于那圈空边，透明行被划进**可拉伸的中段**。修法两条：`tools/trim_alpha.py --apply` 按 alpha 包围盒裁掉空边（9 件：按钮族 7 + `panel_sub` + `avatar_frame`；`bar_track` **故意不裁**——它的 `10 17 10 17` 档按 512×64 量过、HUD 四条已目检，裁了要连档一起重验），切片档改**百分比** `32% 22% fill`（按钮族源件从 226×256 到 540×256 差得远，像素档一档通用不了；32%/22% 是照 `btn_play` 铆钉占 x≤23%、y≤32% 量的，让四枚铆钉整块留在不拉伸的角区）。防复发：`slice_sheet.py` 新增 `--tight`（跳过方化），量取值 `tools/measure_plate.py`、看布局 `tools/ascii_alpha.py`、体检 `trim_alpha.py --check`（任一边透明边 >6% 就是雷），判据落 STYLE-SPEC §9「板件不许留透明边」 |
| 同上 | 翻案：英雄养成五入接回板 | ✅ 5 键上屏 | `.btn.blue.hot`（技能/天赋/升星/武器/核心，61×61）原来被 `CITY_BUTTON_PLATE` 显式跳过，写的理由是「板只盖住中段一条，⚡ 与「技能」两行露在板外」。这条观察是真的、**归因是错的**——"板只盖中段一条"正是上面那个透明边 bug 的表象，不是宿主太矮。裁紧改档后重测：61 盒高减两侧 11px 边框剩 39px 平牌面，两行内容放得下，于是接回 `ui/button/btn_cancel`（截图 `gen-output/platefix_five.png`）。另两处跳过经复核**仍然成立**：`.game-button.sm` 46×22 比板厚四倍还小；`.arrow` 压在深色实景照片上贴板会融进背景 |
| 二十四轮 r31 `gen-output/r31_20260921_052334.png` | 容器底板族 7 件（导航格两态 / 二级页签 / HUD 功能钮 / 装备槽 / 建筑卡 / 入口卡） | ✅ 七件落盘接线 | 先跑一遍全量表面普查（`tools/audit_ui_surfaces.mjs` → `ui-surfaces-0921.json`）再决定出什么图，不再"想到哪补到哪"。剩下最大的无图面按可见度排：底部主导航 5 格（每页都在屏，106×93）、基地页 8 张建筑卡（**整页九宫格数为 0**）、英雄页装备 6 槽、二级分类页签 8 签、行动页 2 张入口卡、战斗 HUD 3 枚功能钮。一张 3列2行表出齐（模型多给了一枚近方件，正好按宿主分给 HUD 钮与装备槽两处，不做复用）。**这是 `--tight` 第一次实战**：切完 `trim_alpha.py --check` 仍有 5 件超 6%——原因是 tight 的留边按**最长边**算（722×320 的板上下各垫 43px = 短边的 12%），等于把刚修掉的坑换个尺寸再踩一次；改成按**短边**算后七件全过。接线走新表 `UiPlate.SURFACE_PLATE`（不并进 `CITY_BUTTON_PLATE`：导航格在 `.viewport` 之外，两套扫描范围不同） |
| 同上 | 换深色板之后的字色翻亮（`.plated` 标记） | ✅ 四处 | 板子把浅底换成深色金属板，原先按「浅底深字」写的四处文字当场读不出来：建筑卡 `LV.0`、导航格页签名、二级页签名、装备槽槽名。做法是 `nineSlice` 在贴图真的落上那一刻给宿主打 `.plated` 类，CSS 末尾按这个类翻亮——**色不写进 `nineSlice` 的内联 color**，因为内联会在缺图回退时把旧配色一起盖掉。两处踩坑记录：① 选中态导航格是琥珀金板，页签名照旧写 `--c-gold-dk3` 实测只有约 2.8:1，改深墨蓝 `--c-navy-3` 才到 7:1；② 装备槽槽名 `.sname` 自己带色（两层各一条），改父级 `color` 继承不动它，必须点到 `.slot.plated .sname` 这一层 |
| 二十八轮（无新出图，纯修普查账） | 三处「内容宽 > 盒宽」：资源胶囊 / 英雄名 / ⚡图标 | ✅ 两处真缺陷修掉，一处判定为误报 | 二十七轮收尾时用户挑了这条继续。**先定位根因再动手，结果三条里没有一条是"字太大"**：① **资源胶囊是误报**——`tools/measure_overflow.mjs`（本轮新建）量出芯片里的文本只占 65.6px、盒子 139.8px、左右各余 37.1px，那 3.2px 出界是 `.res::before` 那块 `skewX(-12deg)` 斜切板画到盒子外面的（tan12° × 板高一半 ≈ 3.1px，与实测吻合）。板子本来就该斜出去，两板之间还有 4.2px 缝，视觉上没撞——**不改**。② **英雄名是真截断**——编队条 `.hero-roster .hpick` 里那枚 🔒 锁徽上一轮为了"不遮字"改成随行内联，可它是 flex 项，那 27px 是从**名字头上**扣的：六个字的名字只剩 58.8px 可用、要 80px，`text-overflow: ellipsis` 把「狙击手·苍鹭」截成"狙击手…"，**截掉的正好是唯一能区分四个英雄的后半截人名**。改成把锁徽钉到立绘右上角（`.pic::after`，往里收 1px 不出格）：不再占行内宽度，名字拿到 86.3px，四个名字全部完整；语义上也更对——锁本来就是"这一格没解锁"的标记。③ **⚡图标是偏心**——`.hot .ic` 的盒与字号同尺寸（36 的方盒配 36 的字），但 emoji 字身由字体给，⚡ 实测占 49.4px。**这条踩到一个 CSS 反直觉**：先按常理加 `text-align: center`，量下来纹丝不动（还是左出界 0 / 右出界 13.4）——`text-align` 只分配"剩余空间"，一个比行盒还宽的词没有剩余空间，浏览器照旧从左边起排。换成 `display: flex; align-items: center; justify-content: center` 才对，负空间由 `justify-content` 对半分，实测左右各出界 6.7px、图标回到视觉正中（🛡️ 🎁 同一条一起吃掉）。**自测口径升级**：`scrollWidth > 盒宽` 这个普查判据分不清"文字被截断""emoji 对称出界""伪元素画出去"三种情况，新工具用 `Range.getClientRects()` 量文本真实墨迹矩形，把三者分成 截断 / 偏心 / 对称出界 / 非文字 四类判定；六个页签全量跑完，截断与偏心两类**归零**。 |
| 二十七轮 r34 / r34b（一张 3 列表 + 一张补件） | 战斗表现件余 5 件（光柱 / 暴击底纹 / 传送门 / 金币爆开 / 激光束）的表现代码 | ✅ 五件建码 + 落盘 + 逐件无头目击 | 接着二十六轮把「缺表现代码」那五件清完，顺序仍是**先建消费点、再落图**。**五条落点各自的形状**：升级光柱 = `LevelUpPanel` 排在选卡三张前面的 `Glow` 节点（`setSiblingIndex(0)`，opacity 封顶 150，再亮把卡上技能名洗白）；暴击底纹 = `DamageNumber` 里排在字**前面的兄弟节点** `WordBg`（Cocos 的 UI 父节点自身组件先画、子节点后画，挂在宿主自己身上会盖住字），只有暴击那一跳才启用；传送门 = `BattleManager._spawnPortal`，只给侧翼切入那 30%（顶部下压那 70% 不是"钻出来"，放了读成 bug），门高 = 怪半径 ×4.2、opacity 230；金币爆开 = 通关结算第三枚芯片里的 `.clBurst`（`z-index:-1` 垫在「+N」后面，失败面不放）；激光束 = `HeroCombat._drawBeamArt` 叠在 `drawBeam` 那三层程序化束之上（照 `Bullet._applyVisualAsset` 的"只加不减"，且 `clearBeam` 必须顺手关掉贴图节点——Graphics 不重画自然消失，Sprite 是常驻节点不关会留一道悬空光束）。**两条画布通用坑**：① 父节点先画、子节点后画（底纹/光柱都因此不能挂宿主自身）；② `UIOpacity` 不跨节点级联，飘字那三层各挂一个、一起淡，回池只挂一层。**新增自测工具** `tools/fx_probe.mjs`：这五件各自只活 0.1~0.25 秒，定点截一张几乎必然错过，所以改成 ① 从 SystemJS 把引擎模块取出来（构建产物的 import-map 里 `"cc"` → `cocos-js/cc.js`）遍历场景图数节点、目击即截图，② 用游戏自带的 GM 面板驱动流程（升级 / 清屏 / 下一波），不必真打一整局，也不必往游戏代码里加调试日志。**r34 表本身返工过一次**：prompt 写"vertical light column"被模型做成一个空心环，重画时把反例写进 prompt（"不许画成环形、椭圆圈、门框或任何闭合轮廓"）才得到实心光柱 |
| 二十九轮（无新出图，纯改皮） | **结构收敛 Step1：页面底翻成暗场景，青瓷层的平灰卡片语言整套换掉** | ✅ 五页不合格计数 8/6/5/3/4 → 全 0 | 用户五点批评（①方块感 ②缺五页背景 ③残留边框/切图感/有无美术夹杂 ④字在板里看不清 ⑤规划不足、页页像半成品）之后定的四步，本轮做第一步。**先量再判**：`tools/contrast_audit.mjs`（本轮新建）把每个有字元素的字色与背板合成色算 WCAG 比值，五页基线 home 8 / 商店 6 / 英雄 5 / 基地 3 / 行动 4。根因不在任何一张图，在皮肤层：青瓷层（`--pw`）是 `prototype-assets/interface.css` 那套浅色休闲皮的 1:1 翻译，**且没有媒体查询、永远覆盖在深色基准层之上**，所以整个 UI 实际是"浅皮 + 贴了几块暗金属板"，底下还铺着 `#e6eef3` 一张平灰纸。商店页实测 27 个面里 21 个有九宫格板——板不是太多，是**底板是灰的**。① 页面底翻暗：`#homeUi` 与 `.viewport` 改 `--c-scene-1/2/3`（UiTheme 本轮新增五个令牌，全部落在既有 navy/deep-teal 家族同档，不引入新色相），`.safeBand`、`.topbar`、`.tabbar` 三条通栏跟着翻。② **平灰卡片语言整套换成暗槽**：`#dcdcdc`/`#ccc`/`#d3d3d3`/`#c9c9c9`/`#b9b9b9` 这一批是原型没换肤的占位灰，不是青瓷皮的设计色——英雄页背包条、商店两张主推卡与货卡、基地页建筑名牌与局外强化条、行动页挑战场地面/资源副本/远征行、英雄选择条、装备六槽、底部四快捷，逐块改成 `--c-scene-1/2` 凹暗格 + `--c-scene-line` 细边，字色按"暗板亮字"翻 `--c-text*`/`--c-cream-*`/`--c-gold-hi`。③ **弹层保持浅纸**：`.panel`/`.mbox` 一族不动，同一个类名两处都出现的（`.mSub`、`.game-button`）只给页面那一处开选择器，不碰公共那条。④ 清"多出的线条"：营地路面的色原先写死在 SVG 的 `stroke="#b8b8b8"`/`"#eee"`（presentation attribute 低于 CSS，改成 `.map-roads .road/.dash` 走令牌）；挑战场那圈椭圆围栏在暗地上只剩两段露头的弧，整条撤；`.slot.empty`/`.slot-avatar.empty` 的 dashed 删掉（暗页上虚线框读成网页线框，游戏口径是"凹暗格 + 弱亮加号"）。⑤ **方块感按证据改判**：原计划"只留主 CTA/导航选中/弹层标题条三类有板"，实测**不成立**——把 `.hot` 一族撤板会露出 emoji 裸底，正是用户上一轮骂过的"有美术和没美术夹杂"；真正的方块感来自平灰孤岛 + 四块同形板无缝并排。于是保留板，改成 `.milestones`/`.action-daily`/`.dungeon-row` 三处让出 5~6px 缝（里程碑那缝正好看住底下的进度轨）。⑥ 新增 `.plated` 两条（主 CTA 金牌上白字只有 2.4:1 → `--c-navy-7`；蓝板上深字 → `--c-cream-1`）。**审计器本身修了两处会误判的地方**：只读 `backgroundImage` 会把所有九宫格板误报成"字压在平灰上"（板走 `border-image` 且 `nineSlice` 把 background 置成 none）；祖先链上的 `opacity` 要连乘，且 WCAG 对 inactive controls 豁免——改成单列一组报出来（看得见、不计入不合格）。收尾前又用 `plates` 模式抓到一处**特异性压制**：`.hero-quick .btn` 翻亮无效，因为容器实际类名是 `fcol hero-quick`、`.fcol .btn.blue`（1 id + 3 类）压过它（1 id + 2 类），六枚养成入的标签一直停在 `#243e4d`。 |
| 同上 | 保留决定：`.hot` 一族继续贴板 | ✅ 记档 | 见上一条 ⑤。撤板这条路**判过、不走**，理由是它会制造用户已经点过的那类夹杂；剩下的方块感交给第二步（五张场景底图）去吃——背景一上来，同形板就从"网格里的格子"变成"摆在场景里的物件" |
| 二十九轮 Step2（一张新图） | 主城页面底 `scenes/hub_camp` | ✅ 一张共用、五页同世界 | 批评第 2 条「缺少 5 个主界面的背景，还是空白背景」。**没有出 5 张**：五页各画一张正是"通用性很低"的那个毛病，而且出图口径逼着每张的中段都得留空（那一带要压 UI），五张留空的中段会显得五页都缺东西。改成一张竖构图营地场景（1024×1792 生成 → `land_bg.py` 新增宽高两档参数 → 1080×1920 JPG、149KB），口径=地平线压在下三分之一、中段一整条低对比留空、四角与顶沿重暗角、暖色工作灯是画面里唯一亮点源。接法走 `_tex` 在 DOM 上写 inline「遮罩 + 图」，CSS 那两层渐变留作缺图回退。**实测三件事**：① 遮罩必须留——场景中段那道琥珀雾亮带是整幅最亮处，弱亮字压上去会掉到 4.5:1 以下；② 光有背景不够，商店/英雄两页整屏被 `.shop-offer`、`.bagBar` 两块不透明暗槽盖住，看不出换了底，于是新增 `--c-scene-panel`（scene-2 的 .82 透明档）给这类"一整屏都是它"的面用，读起来从"UI 摆在纯色纸上"变成"UI 摆在场景里"；③ 只有 基地页 原本就能透出场景（建筑卡之间有缝）。**护送页仍透不出来**——那一页中间是关卡照片，它要的不是"背后还有一张底"而是"照片就是页底"，那是布局改动不是换图，另开一轮，已记进 §2.1 下一步 |
| 二十九轮 Step3（无新出图，纯布局） | 护送页关卡照片改成通栏出血 + 上下沿渐隐 | ✅ 硬边矩形消失 | 接 Step2 那句"护送页要的是照片就是页底"。**没有把照片搬出 `.stage`**：DOM 上 `.stage-scene` 是 `.stage` 的子节点，桌面基准层要靠它的 `inset` 给两侧快捷列让位，搬走会连带破桌面布局；而手机侧真正的问题只是"四条硬边"。所以只动青瓷层两条：`inset: 6% 140px 0` → `inset: 0`（左右各 140px 的内缩本来就是给两侧快捷列让位的，现在列直接压在照片上——列本身是木箱板，比让出来的那条暗缝更像"摆在场景里的物件"），再加 `mask-image: linear-gradient(180deg, transparent 0, #000 14%, #000 76%, transparent)` 把上下两道横缝也化掉，照片溶解进暗场景。不支持 `mask` 的浏览器退化成改动前那个硬边矩形，不会更差。`.stage-caption`（战力/推荐）是 `.stage` 的子节点、不在被遮罩的 `.stage-scene` 里，所以它仍然压在渐隐后的近黑底上。**五页对比度不合格计数仍为 0** |
| 二十九轮 Step4（一张新表 6 件 + 挖出一条潜伏缺陷） | 空态不再留空板：装备六槽暗色剪影底纹、背包空态改用在库件 | ✅ 六件落盘接线，`ico_empty` 第一次真的上屏 | 批评第 ③ 条「有美术资源和没有美术资源的组件夹杂在一起」的另一半：**空着的位置也是位置**，它比贴了错图的组件更容易读成"这格没做"。两处实测最扎眼——英雄页装备六槽（头盔/护甲/腕甲/护腿/手套/战靴全空，占右半边一大片暗方块）、背包空态（整片空暗格里只有一行 13px 小字）。① 六槽各垫一枚这一位的**单色低对比剪影**（r35b 一张 3列2行表出齐，槽空时才建 `.slotGhost` 子元素，虚/实构成同一格的两种状态，不出二态图），契约表 `UiPlate.SLOT_GHOST_TEX` 与 `STATUS_TEX`/`MISC_TEX` 同族摆放，接线收进 `HomeUiCore._slotGhost` 一个方法、调用点一行。**第一版 r35 整表作废重出**：模型给每枚剪影套了一圈方框，而宿主本身就是一块 `ui/panel/eq_slot` 金属板，框套框正复现"方块感太强"——判据已进 §8 坑表：**宿主自带板的图标位，出图表头就要显式禁框**，光写"单色剪影"不够。② 背包空态零出图成本：改走 `_popEmpty(text, undefined, 'ui/ico/ico_empty')`，与弹层空态同一件东西，两处空态长得一样。③ **顺带挖出的真缺陷**：`ico_empty` 自 Step2 登记进 `_popEmpty` 默认值以来**从来没有显示过**——`UiPlate.icon()` 贴上图那一刻按纪律摘掉同位 glyph，摘完这个 `div.ei` 就没有内容，块级空元素高度 0、flex 交叉轴按内容定宽也是 0，图无处可画；emoji 那一支不受影响（字形自己撑得住）。补上方框尺寸与 `display:grid` 才第一次上屏，这条同时解释了为什么 Step2 那行写的"先摆占位字形、图到位摘掉"看起来对、实际是空的。④ 自测取景英雄页：六格剪影目检通过（凹印感、不满不抢已装备那支的 emoji），空背包那枚 `ico_empty` 看得见——它是只空金属箱，库里没有背包类图标，语义对得上"空容器"，不为这一格另出图。⑤ 本轮还掉一笔拆分账（见 §2.2）：Core 上一轮提交时就已越过预算 7 行，这轮把弹层契约拆成 `HomeUiPop.ts` 才真正还上 |
| **大图标族（一类清掉主城 29 处 emoji 插画位）** | 先建普查工具量清单，再用 r36/r37/r38/r39 四张表 19 件一次进完 | ✅ 无贴图的大号 emoji 位 33 → 4，剩下 4 处全是**判过不该出图**的 | 批评第 ③ 条「有美术资源和没有美术资源的组件夹杂在一起」的最后一块：主城还有一大片"拿大号 emoji 当插画"的位置，和已经上图的件混在同一屏里。**先量再判**：新建 `tools/audit_emoji_slots.mjs`（叶子元素 + 文本只有一个图形符号 + 字号 ≥18px + 没有 background-image，按宿主链去重），实测五页 **33 处**——基地 8 张建筑卡（图位 187×104，单页最大的插画群）、行动页 13 处（挑战场两张 205×151 入口卡、资源副本 4 条、远征、巡逻、日常四快捷、载具改装）、商店 2 处、英雄 1 处、护送 1 处。r36 一张 4列4行 16 格出齐基地 8 + 副本 4 + 入口 2 + 远征 + 巡逻。⚠ **切片口径翻车一次**：这批是等距小场景插画、每件脚下带一片泥地，默认 `--dilate 6` 把同一行四件焊成 4 个连通域（整表切不开），改 `--dilate 2` 才 16/16 干净——§8 原来只写"粘连就往大调 dilate"，这条反方向的坑补进去了。⚠ **一格判死后补出两次**：「强化石副本」连续两版都画成发光紫水晶（r36 第 10 格、r37 第 1 格），与同排「晶体矿脉」在 55px 里读成同一个东西，而那一排给玩家做的选择正是"打哪个副本"——撞形是功能缺陷，比缺一条更糟；第三次把槽位名从"强化石"改成"砖石堆场"并允许写一句材料才拿对（r38 单格表）。这是 §8「prompt 里不写材质」的一条**有记录的反例**：槽位名太抽象、连续两次撞形时，第二次要把材料说死。⚠ **取景抓到一条尺寸判据**：r37 给巡逻（47px）与载具改装（27px）的是等距场景，落到小格子就是一团深色糊影、还跟暗底板融色，同一排的单物件件（卷轴、奖杯、宝箱）反而清楚——r39 按"单个物件、不画场景不画地面"重出哨塔/改装车/勋章三件并**换图不换 key**，判据定死为 **<60px 的位一律单物件，≥60px 才允许场景插画**。另 r39 首版三件里两件部件相接、`--dilate 1` 都分不开，重出时加一句"任何两件不许接触"才切开。② **四处零出图**：行动页日常四快捷里的签到/任务/礼包、护送页脚「掉落」、远征行那枚 🚚、以及商城材料格——素材早就在库里，缺的只是 `mkDaily` 的一个 `tex` 槽；给 §9 补了反面判据：**在库件也要逐格核宿主，出过图 ≠ 接上了**。③ 两处**故意不接**：「成就」不复用 `ico_trophy`（那件已挂页脚「排行榜」，两个语义共用一个符号等于把「成就」和「排行榜」压成同一个入口，所以给它出了专件）、英雄页「技能」按用户明令保留 emoji。④ 收尾复普查剩 4 处：技能 ⚡（明令不换）、开始护送 CTA 里的体力 ⚡ 与商城「限时特惠」的 ⏰（随文小符号，§9 判据①）、以及普查器自己的一个假阳性（`\p{Emoji_Component}` 把战力数字"800"也算成 emoji 位，已收紧正则）。自测：基地/行动/商店/护送四页无头取景逐面目检——八张建筑卡、两张入口卡、四条副本、页脚四键全部上图；未解锁那两张（雷达站、无尽护送）由既有 `.locked` 的 opacity + grayscale 压暗，锁定态仍然认得出 |
| 三十轮（零出图，纯撤板） | **用户四条批注：侧栏 / 里程碑 / 页脚两键去背板，底导五格并成一块** | ✅ 三处板撤掉、里程碑加进度轨、导航一条整板 | 大图标族把图补齐之后，"撤板会露裸 emoji"这条保留理由不再成立，用户逐条点名四处：**去掉背板，只剩图标和文本**（侧栏一列、护送页脚巡逻与掉落）、**去掉背板，增加进度条**（里程碑三档）、**去掉页签间隔，五格放进一个背板**。① 前两处零成本：`CITY_BUTTON_PLATE` 加三条 `key: null`（顺序在通配 `.hot` 之前），图标同时放大（侧栏 49→60 宽 / 24→32 图，页脚 60→44×--pw，均含基准层同档），**不动 DOM 不动代码**。② 里程碑那条进度轨是真的：`.milestones` 底下原来只有一条装饰 `border-bottom`，现在换成 `::before` 凹槽 + `::after` 金色填充，填充宽度由 `HomeUiStage._refreshBattleChests` 写进 `--mile`（达成或可领都算推进，实测 2/3 = 0.667）。③ 导航五格并一块的代价记进 §9：**一块板上没有"哪一格换了板"**，选中态从换图改成 CSS 三件事（凹下深色底 + 顶部金槽 + 字色转金），`tab_plate_on` 因此下架到 `stock/nav/` 并进 `RESERVED_SLOTS` 写明理由。④ **自测抓到一处"撤了板又长回一块"**：无头取景里可领取那格后面有一块硬边深色矩形，先怀疑残板，用 `tools/probe_milestone.mjs`（本轮新建，一次性探针）把该矩形区里所有会画东西的节点连同伪元素全列出来——`.milestone` 底色 `rgba(0,0,0,0)`、`border-image: none`，**没有任何板**；再用像素法在同一行内取点与板外取点（`(13,20,28)` vs `(13,20,28)`）证明盒内盒外同色。真凶是基准层给**整格**（163×83）挂的 `huiChest` 金色脉冲：一圈 11px 的 `box-shadow` 把里面衬成了暗板，撤板轮把它请回来了。改成只给箱子图标脉冲之后**仍剩一小块方框**（`.ic` 的 53×43 盒），因为 `background-size: contain` 的图不满盒，而 `box-shadow` 描的是盒；最终改成新的 `@keyframes huiChestSil`——`filter: drop-shadow` 吃渲染后的 alpha，光晕沿箱子轮廓走，方框才真的消失。**判据**：撤板之后要复查"还有什么画在整格上"，`box-shadow` 脉冲是最容易漏的一种，因为它不是背景；而**给贴图件做"在亮"的提示，一律用 `drop-shadow` 不用 `box-shadow`**，后者永远是个矩形。⑤ **收尾审计先报了一次假红**：`contrast_audit` 在 home 页从「量 48 个 / 不合格 0」变成「量 60 个 / 不合格 3」，那三条是顶栏三枚 `.tinyIcon`（邮箱/公告/设置）被判成"黑 emoji 压暗底 1.17:1"。探针实测这三枚**自带 `background-image`、`textContent` 已经是空**——图早就上了，只是 `_tex` 是异步排水的（`_pendingTexTimer` 每 400ms 重排），审计赶在贴图之前扫，就把还没摘掉的占位 emoji 连同"没有背图"一起量了。改成**连量两次、两组计数不动才认**（`contrast_audit.mjs`），home 立刻回到 48/0 并与改动前那次运行逐字一致。**这不是把判据放宽**：阈值、豁免规则、计数口径一个字没动，动的是"什么时候量"。 |
| 三十一轮（一张新表 4 件 + 一处结构） | **护送页中心从"一张照片"改成立体船坞**：地面 / 平台 / 载具 / 前景碎石四层叠成一个场景 | ✅ 四件落盘接线，中心第一次有"主角物件" | 拿《向僵尸开炮》的主界面逐条对标，第一条差距不是画质而是**结构**：人家中心是一座立体维修坞（平台 + 吊车 + 斜坡 + 停在里面的车），中心是"道具"；我们中心是一张卡车照片，中心是"布景"。这两种读法一眼分得出钱花在哪，所以这一轮改结构不改图。**做法**：r40 一张 2x2 表出四件（一次成表是必须的——这四件要叠成同一个世界，逐张生必然漂移，§8 那条"单次生成内部自洽"在这里比图标族更成立），槽位名只写场景角色，另外补一句**空间关系**（地面在最底、平台压上、车停在平台里、碎石在最前），不写这句模型会按四个互不相干的道具画、透视对不上。切片 4/4 干净（`--dilate 6` 默认档就够，四件间隔大）。DOM 上 `.dock` 挂在 `.stage` 而**不是** `.stage-scene` 里——场景那一层底沿有渐隐遮罩，会把平台的座子吃掉；按 DOM 顺序排在两侧快捷列之前，所以船坞压得住照片、又不会盖住签到列与翻页箭头。四层一律 `background-size: contain` + 钉盒子下沿，调构图 = 调 `left/right/bottom/height` 四个百分比，图永远不变形；缺图那层什么都不画，整块自动退回"一张照片"的老样子，所以能一层一层补、不需要开关。碎石**同一件用两次**（右那枚 `scaleX(-1)`）比出两张便宜。⚠ **取景抓到一处"两个卡车"**：底图 `center 57%` 正好框住照片里那台车，船坞自己又有一台，同屏读成"照片那台是真的、平台上那台是贴纸"。改成 `center 22%` 露天际线与路牌——章节身份还在，车让给船坞。⚠ **顺带补一层暖色主光**（`.dock::before` 的琥珀 `radial-gradient`）：平台、载具、照片原来各是各的色温，叠上去仍像"四张贴图"，加一个从下往上的共同光源才读成"一个场景"。**这条对应差距第 4 条，是 CSS 活不是出图活** |
| 三十二轮（一张新图 + 挖出一条潜伏缺陷） | **整页底铺满护送页（含顶部组件）**，顺带查出"页面底从来没画出来过" | ✅ 一张图从状态栏铺到底导；五页底图第一次真的上屏 | 用户拍板"背景才是最首要的"，圈出屏幕最上方那条 36px 的平色带问顶部组件怎么办。**根因有两层，第二层才要命**：① 底图贴在 `.viewport` 上，而顶栏与安全区带是它的**兄弟节点**，所以那条带子永远涂着自己的平色、与世界无关。改法是底图上移到公共父节点 `#homeUi`，并把移动档 `.safeBand`/`.topbar`/`.viewport` 三层底色撤成透明（顺带撤掉顶栏那道 1px 下划线，以及 `.stage-scene` 的涂色和它的青色渐隐 `::after`——画面已经交给整页底）。桌面档反向补一笔：那边 `.viewport` 仍不透明，`.safeBand` 不补同色底就会在顶栏上方露出一条照片边、读成贴图错位。② **`UiPlate.frameUrl` 返回的已经是包好壳的 `url(...)` 整串，而这一处调用又套了一层 `url("…")`**，得到 url 里嵌 url，浏览器判**整条** background-image 非法，于是只剩遮罩渐变在画——画面看着"只是暗了一点"，`backgroundImage` 读出来照样非空，普查与比值审计都过，**从登记那天起一直空到这一轮**。全工程另外 19 处 `_tex` 回调都是直接赋值，所以别处全好、偏偏背景是空的。这条同时解释了用户为什么第 2 次点名"背景还是空白"：上一轮我们以为自己把背景做好了。③ **出图**：护送页第 1 章一张新底 `scenes/bg_road`（1024×1792 → `land_bg.py` 1080×1920 JPG、119KB），构图硬要求写进提示词——顶沿五分之一必须是压暗的天空（要盖头像与资源数字）、中段一整条低对比、地平线在下三分之一、底沿四分之一收进暗色前景、唯一高光是低处几盏暖橙工作灯（和船坞那层琥珀主光同色温）；并明令**画面里不许有主角车辆**，否则和船坞那台车撞成两个卡车。第 2~5 章不再另出，直接复用战斗侧 `STAGES[].backdrop` 那四张竖图（720×1280，本来就是满屏比例）。④ **新的审计盲区**：底图铺满全屏之后压在它上面的字全算不出背板比值，五页"算不出"计数 33/26/38/22/31 → 39/32/44/28/37，比值审计在这几页覆盖力下降、只能靠取景目检——**#44（按截图取背板像素）从"锦上添花"变成这一页继续加美术的前置条件**。⑤ 两条判据进断言与规范：`_tex` 给什么就用什么不许套壳（`check-art-manifest` 新增 5.9 扫这个写法）；多层 background 不能只看"非空"，渐变层会掩盖图片层失效（`tools/probe_pagebg.mjs` 打 inline/computed 尾巴 + 那一点的命中栈） |

### 2.2 行数棘轮欠账（`check-split.js` 的 `SPLIT_DEBT`）

`HomeUiCore.ts` / `HomeUiHeroes.ts` 用棘轮记预算（目标 1800）。**红线：只许把文件拆薄，不许把预算改大。**

图标第一批接线给两个文件各加约 8 行，Heroes 破了 2490 预算，Core 顶到 2240 零余量。按红线**真的删死代码**，分两笔提交还掉：

| 提交 | 文件 | 删了什么 | 净减 | 结果 |
|---|---|---|---|---|
| `18084c2` | `HomeUiHeroes.ts` | 18 行拆分残留的未使用 import | 18 | 2494 → 2476 |
| `e2d1f85` | `HomeUiHeroes.ts` | 零调用方法 `_affixBlock` + 随之失效的 `affixColor` 引用 | 22 | 2476 → **2454**（余 36） |
| `e2d1f85` | `HomeUiCore.ts` | 12 行未使用 import（含 `ccclass` 样板）+ 零调用方法 `_openModal` | 50 | 2240 → **2190**（余 50） |
| Step5 提交 | `HomeUiCore.ts` → `HomeUiMall.ts` | **不是删，是往下游挪**：`_popWarn` 在 Core 内零调用、19 个调用点全在 Mall 及更下游，挪一层照样人人可用。（2239 是本轮开工时的实测值——上面那笔还到 2190 之后，进度条与角标几轮接线又把它涨回去了，棘轮只保证不再涨） | 5 | 2239 → **2234**（余 5） |
| 空态轮（2026-09-21） | `HomeUiCore.ts` → **新文件 `HomeUiPop.ts`** | 弹层入参契约七个 type/interface（`PopTier`/`PopSize`/`PopText`/`PopCta`/`PopRowOpts`/`PopAttrOpts`/`PopOpts`）整块搬出——纯类型、零运行时代码，Core 只留「怎么画」。五页 + Core 的 import 随之改指新文件 | 100 | 2255 → **2155**（预算 2240，余 85） |
| 同上 | `HomeUiCore.ts` | 顺带删掉一条指向已迁走的 `CHAPTER_THEMES` 的孤儿注释（就是下面清单里那条的一部分） | 2 | — |

⚠ **这一轮同时记一笔流程账**：Core 在上一轮（进度条族）提交时就已经是 2247 行、**越过 2240 预算 7 行**，
也就是收尾链带着一条 FAIL 进了库——那轮把 `check-art-manifest` 与 `check-popup-ux` 补进链，
却没看 `check-split` 的既有断言。本表的红线是「只许拆薄」，所以越账之后只能拆，不能把预算改大。

两笔合计 90 行；`e2d1f85` 单笔 72 行（该提交信息里写的「91 行」把上一笔的 18 行重复计入了，
以本表为准——历史已推送，不为一个数字去强推改写）。

**下一轮还要行数时直接从这张剩余清单取，别重新扫**（已全目录 grep 核过零引用、且确认无 check 断言钉住）：

| 位置 | 可省 | 说明 |
|---|---|---|
| `HomeUiCore.ts` `_popOpen` / `_closeTopMask` | 3 + 7 | 都是零调用的旧弹窗出口包装 |
| `HomeUiCore.ts` `_safeBandEl` 字段 | 3 | 只写不读；安全区实际靠 `_applySafeArea()` 写 `--sat/--sab` 令牌驱动 |
| `HomeUiCore.ts` 六条孤儿/历史注释 | 6 | ~~含指向已迁走的 `CHAPTER_THEMES` 的文档注释~~（空态轮已删）；余下含失效的「商店页」分区头、描述已不存在的「输入 CONFIRM 二次确认」 |
| `HomeUiHeroes.ts` 三条孤儿注释 + L1–L2 装饰器样板 | 10~11 | 注释指向的 `_openHeroCoreModal`/`_openWeaponModal`/`_openSkillModal` 全工程零命中 |
| `HomeUiHeroes.ts` 背包穿戴行渲染两处重复 | ~14 | `_openEquipSlotPanel` 空槽分支与页签 2 分支逐行同构，只差回跳 tab |
| `HomeUiHeroes.ts` 工坊按钮造两遍 | ~8 | 页头「工坊」与背包行「合成」同 title 同动作；**砍其一是产品决定** |

**两条被锁死的肥票（想拿要先动 check，属另一笔账）**：`HomeUiCore.ts` 的 `_openSheet`(39) + `_openResult`(36)
全工程零调用，本可删 75 行，但 `tools/check-ux-layout-bundle.js:14/16` 正向断言这两个标识符与 `sheetGrip`
必须在构建产物里。**该脚本已不在 AGENTS.md 的收尾验证链里，且自身当前就报 2 FAIL**（`_baseMetaEl`、`floatRail`
早就不存在）——要不要清那条清单来解锁这 75 行，是独立决策。

**绝对不能碰的锚点**（都有 check 断言钉着）：`_pointerReady` 及其上方解释指针捕获的注释、
`_wireBagDrag(` 必须恰好出现 2 次、五入按钮的 const 名与 `fcol.appendChild` 顺序、`starEntry` 类名、
`_renderSkillCards`/`_openForgeModal` 落位锚点、`_stamTick` 的 `private` 关键字、
`HomeUiStyle` 的 `.affix*` 规则（`check-split-bundle.js` 有 `bundle-css: affixVal` 进包断言，
删了 `_affixBlock` 也**不能**连带删样式）。

### 两版对照结论（六轮）

| | 用户版 `icons.png` | r8（新写法 + 荧光绿底） |
|---|---|---|
| 可切片 | ⛔ 深灰底切不出 | ✅ |
| 造型密度 | 高（骷髅书/靶心剑/双人徽章），56px 会糊 | 低一档，轮廓更好读 |
| 风格 | 更暖、更废土手绘 | 偏金属灰，圆环框变多 |
| 语义准确 | `ui/ico/ico_add` 出成资源堆 ✗ | 加号 ✅，但试炼/无尽撞成两个骷髅 ✗ |

**没有哪版全面胜出**，所以流程第 5 步的分流是必须的：整表挑不出更好的那一版直接全用，
要按格取。台账清零前不进下一类。

## 3. 交付顺序（阶段 = 批次的验收打包）

| 阶段 | 含批次 | 交付验收 |
|---|---|---|
| P0 工具固化 | — | `--rect WxH` 非方化切片落地 + 九宫格四角契约入 SPEC + 基线 tag |
| P1 主城与弹层 | 批0 + 批1 + 批2 + 批3 | 框徽直取上屏；弹层底板/按钮/标题全贴图化；全弹层走查 |
| P2 战斗主循环 | 批7 + 批8（不含主题×4） | 进战三截图验收（决策点：走帧保留） |
| P3 内容图标 | 批6 + 批10 + 批4 | 英雄/技能/装备弹层走查 |
| P4 资源进度与特效 | 批5 + 批9 + 批8 主题×4 | 顶栏进度条贴图化 + 结算/升级走查 |

## 4. 共性纪律（每批强制）

1. 一类一表一次成型；prompt 只报**功能名**（写「图鉴」，不写「图鉴卷轴」——形象交给模型设计，
   它的设计能力优于替它设计）；底色**点名 `#00FF00`** + 格间留隙；2K 起步。
2. 出图先与基准图2并排过验收清单（STYLE-SPEC §6），再切片。
3. 换图不换 key；新 key 先登记 AssetLib MANIFEST（文件可后到）。
4. 同族 emoji 一并替换收口，不让 emoji 与贴图长期混排。
5. 一批一个 commit：素材 + meta + 接线 + 规范更新同包，可整体回退。
6. 出完逐格进 §2.1 台账：合格的落盘、不合格的 key 留在 `RESERVED_SLOTS` 等补出小表，
   **该类台账清零才允许开下一类**。

## 4.9 对标《向僵尸开炮》主界面的七条差距（2026-09-21，按"改完能拉回多少"排序）

起因：用户拿一款商业游戏的护送/出击主界面截图逐条比对，问"还要怎么做"。结论是**差距不在单张图的
画质，在画面组织方式**——所以这一节记的是"做法"，不是"再画几张"。每做完一条在状态列打勾并回写台账。

| # | 差距 | 他们怎么做 | 我们当时怎么做 | 做法（结构 / 出图 / CSS） | 状态 |
|---|---|---|---|---|---|
| 1 | **中心没有主角物件** | 一座立体维修坞：平台 + 吊车 + 斜坡 + 停在里面的车，车是可换零件 | 一张卡车照片贴在背景上 | **结构 + 出图**：中心拆成四层叠偶（地面/平台/载具/前景），载具单独一张 | ✅ 三十一轮 |
| 2 | **全是面板，没有道具** | 入口是徽章、绶带、盾牌、斜切横幅；资源条是带厚度的斜角金属带 | 约十五处"矩形 + 边框 + 圆角" | **结构**：撤容器（撤板轮已开四条），其余把边框换成道具外形 | 进行中 |
| 3 | **一套图标语言** | 左右两列十几个图标同一光向、同一圈彩色描边光、同一枚红点 | 等距场景 + 扁平卡通 + emoji 三种渲染语言同屏 | **出图**：定一个徽章外形与光向，把全部入口图标按这套重切一遍 | 未动 |
| 4 | **一个暖色主光统一全片** | 所有高光都是橙的，光源是脚下那块发光地板 | 冷蓝底 + 金强调 + 灰金属板，三个色温打架 | **CSS**：给画面定一个主光方向与色温，各层的高光按它走（本轮先在船坞底下补了一层琥珀光） | 起步 |
| 5 | **斜切与厚度** | 条/签/任务横幅端头都是斜的、有 2.5D 厚度 | 平涂圆角矩形 | **出图 + CSS**：资源胶囊、难度段、任务条按"斜端 + 厚底边"重出一族 | 未动 |
| 6 | **选中态是另一个物件** | 选中的「战斗」抬起来一方瓷砖，顶上一道青色标头 + 橙色爆点 | 凹下一格 + 金槽 + 字转金（撤板轮定的） | **CSS + 出图**：选中件单独出"抬起"形态，不换色而是换轮廓 | 未动 |
| 7 | **经营信号密度** | 一屏约三十个入口，红点、倒计时、首充/月卡/限时堆得满 | 克制得多 | **产品**：按「没有落点也要建落点」先造功能位，再谈出图 | 未动 |

**判据**：第 1、3、4 条做完，2/5/6 会顺带掉一半——因为"色温"和"光向"是同一件事，
一套图标语言定下来之后，斜切厚度和选中轮廓都只是那套语言里的两条规则。反过来先做 5/6
等于在没有统一语言的前提下再发明一种方言。

**⚠ 这张表漏了第 0 条，是用户当场补的：「背景才是最首要的」。** 回头看那张对标图，人家一整屏
**没有一处"界面底色"**——顶栏压在天空上、底导压在地面阴影里，所有控件都浮在同一片环境上。
我们当时是"一张图只贴 `.viewport`， chrome 各涂各的平色"，所以无论中间做得多好，上下两头都在提醒
玩家这是张网页。这条排在 1~7 之前，因为**它决定其余六条画在什么上面**：底色不统一，图标语言和
主光方向就都只是局部的事。已按此重做（三十二轮，见 §2.1 与 STYLE-SPEC §7.2）。


| 风险/决策 | 处理 |
|---|---|
| 基准图2 直取件的分辨率上限 | 只直取显示位 ≤128px 的框徽件；按钮/面板/小图标一律重生成 |
| 基准图2 非绿幕底 | 批0 用 edit 模式重绘背景为纯绿（组件保持不变） |
| 怪物走帧风格混源 | 保留走帧（动作资产），单图重生成拉近 |
| 人物整表一致性 | 2×2 大格 + 2K；四张并排比对再切 |
| 九宫格面板契约 | 四角 24px 等宽，量测后配 9-slice；不达标重出 |
| 长条件（绶带/横幅/按钮板）变形 | P0 `--rect` 落地后自然比例切片 |

## 5.5 规范固化轮决策（2026-09-20）

| # | 决策 | 落地位置 |
|---|---|---|
| D1 | **通用件契约层单源**：语义→板、九宫格参数、glyph 摘除规则全部收进 `assets/scripts/ui/UiPlate.ts`；页面禁止手写 `style.borderImage*`、禁止用中文文案正则猜板（旧 `HomeUiCore` 的 `/看广告\|免费\|广告/` 已删，`ad` 成为显式 kind） | UiPlate.ts + STYLE-SPEC §9，checker 逐 key 对账 |
| D2 | **态策略**：一族一件，禁用/选中/按压全部走 CSS 派生（`filter` / `scale` / `active`）；只有语义上真的两态（载具完好↔受损）才整族出双件。页签不出 `nav_*_on` | STYLE-SPEC §10 |
| D3 | **双轨择一**：品质框唯一真源 = `ui/frame/frame_q0~q3`（本轮已接 `.popQ .qi`，CSS 白边降为缺图回退）。`.popQ` 头底色渐变是「品质头」另一件，不冲突、保留 | STYLE-SPEC §9 + HomeUiCore |
| D4 | **段位 ≠ 名次**：`ui/rank1~7` 是段位徽章，**当前游戏没有段位 UI**，2026-09-21 分类迁移时整族移出包 → `art-spec/reference/stock/rank/`（段位玩法开时按该目录 README 拷回并登记 MANIFEST）；排行榜前三（🥇🥈🥉）与 HUD `.statRank` 是「名次奖牌」，新登记 `ui/badge/medal1~3` 预留位，图到位再接，现在不动 CSS | ASSET-MANIFEST §B/§C/§E2 |
| D5 | **预留槽位进代码即采购单**：`AssetLib.RESERVED_SLOTS` 是预留唯一真源（条数不写死在文档里，以该表实测为准——落版图就减、开新槽就加，写死的数字必然过期），预载跳过这些 key（不再白发 100+ 个失败请求）；图落地必须删声明 | AssetLib.ts + check-art-manifest |
| D6 | **在库无宿主件必须收口**：旧一代板（panel_metal / panel_frame / panel_card / card_frame / icon_frame / btn_primary / btn_gold / btn_cyan / chip_dark / banner / banner_orange / btn_round2 / avatar_frame / ribbon_title / frame_bronze~gold / rank1-7 / ico_trophy / ico_lock / ico_achieve / shop_scroll）下一轮要么按 §9 登记宿主接线，要么整族删除，不允许长期「预载但不引用」 | 本节 |
| D7 | **贴图与 glyph 混排收口**：贴图到位由 `UiPlate.icon()` 摘 glyph，缺图保留 glyph 回退；页签青瓷层原 `font-size:0`（会把缺图页签变成空槽）已改为按档显示 emoji 占位，两层口径一致 | HomeUiStyle/HomeUiCore |
| D8 | **在库件必须有归宿**（P0 接线轮）：`check-art-manifest` 新增断言——磁盘每张图要么被代码引用（含 `characters/hero_${id}` 这类模板拼 key 的前缀族），要么在 STYLE-SPEC §9「在库无归宿件」表里有一句说法。引用扫描同时修掉两个口径漏洞：清单自身不再算作"引用"、块注释里的 key 不算引用 | STYLE-SPEC §9 + checker |
| D9 | **接线只接当前世代**：P0 只接 r4/r6 套件件（与新基准同族）；r1/r2 老一代板（panel_metal/panel_frame/panel_card/card_frame/icon_frame/btn_gold/btn_cyan/chip_dark/banner/banner_orange/btn_primary）**不为消化库存而硬塞进 r6 宿主**，一并列退役候选等拍板 | STYLE-SPEC §9 |
| D10 | **主城按钮并入同一板族（范围经实测收窄）**：`UiPlate.CITY_PLATE` + `HomeUiCore._plateCityBtn()` 让 `.btn.gold/.blue/.adBtn/.dark` 复用弹层板，但**只允许 `.btn.big` 这类整幅大键**（已接「前往商店解锁」）；英雄养成五入与商城 `.gBuy` 小胶囊贴板会把文字压在板面上，已撤板回 CSS 底色，等 `bar` 薄板档定档 | STYLE-SPEC §9 薄板档 |
| D10b | **主城缩放令牌补齐**：主城上下文原本没有 `--pu`，`UiPlate` 的 `calc(Npx * var(--pu,1))` 退化成恒定像素（手机上板厚 10px 而非 25px）。现由 `#homeUi .btn` 两层各写 `--pu: var(--hs,1)` / `--pu: var(--pw,2.5)` 补齐——**接任何新宿主前先确认它所在层有 `--pu`** | HomeUiStyle |
| D11 | **四套页签不合一**：`.popTabs`（弹层文字档）与 `.shopTabs/.bagTabs/.chTabs`（主城图形档）布局职责不同，只统一「图标位 + 选中态走 CSS」口径；二级页签按 `ui/ico/tab_*` 六 key 一次成表 | STYLE-SPEC §9 |
| D12 | **进度条与状态图标族的排产闸门**：进度条族 `bar` 档**已定档**（`UiPlate.NINE.bar` = slice `8 fill` / 显示 2px×--pu，依据是量基准图得「描边:条高 ≈1:9」），并新增一条硬门槛：**宿主条高 ≥10px 才可贴**，3~7px 那几条不接；状态图标族 `icons/status_*` **延后**（战斗 buff 无 DOM 图形位，出图会白出），只挂预留不排产 | STYLE-SPEC §9 薄板档 + UiPlate.ts |
| D13 | **旧版 UI 素材归档而非删除**（2026-09-20 用户拍板）：11 件 r1/r2 件移入 `art-spec/reference/legacy-keep/`（不占 `textures/` 契约位、不进包、不被 checker 要求归宿），内容件（技能图标 12 / 走帧 4 / 弹道 / fx / 路面）**留用不弃**；要换回某张旧图 = 从归档目录拷回同名 key 重建 | ASSET-MANIFEST §A/§E |
| D14 | **提示词只报功能名，不报形象**（五轮 A/B 实测）：同一批 20 格，「图鉴卷轴/试炼高塔」写法出的图字面拘谨，「图鉴/试炼」写法让模型自己设计，密度与风格一致性明显更高——模型的设计能力优于替它设计。代价是必须逐格审台账（`ui/ico/ico_add` 按"获取资源"就出成了资源堆）。槽位名要贴真实用途：改叫「加号」就出对了 | STYLE-SPEC §8 生表 + 五轮 A/B 记录 |
| D15 | **底色必须点名 `#00FF00`，不留给模型**：写「纯色背景方便抠图」会被发挥成草绿（通道差 97，与主体绿重叠 → 无解）或深灰（描边本身就是近黑，按距离抠会连描边一起吃掉）。点名 `#00FF00` 后背景侧通道差 ≥229，默认容差直接可切。**不改切片工具去支持深色底** | STYLE-SPEC §8 坑 |
| D16 | **功能图标一律去装饰外框；通用约定符号豁免「无文字」**（2026-09-20 用户拍板）：金属圆环/盾形/徽章底板一律禁止（显示位只有 44~56px，框会挤掉主体），但静音斜杠、警告三角、信息圆牌这类语义形状要保留；信息图标中心的字母 `i` 不算文字违规 | STYLE-SPEC §3 硬规则 + §6 第 6 条 |
| D17 | **一类没清零不开下一类**：整表不合格不等于整表作废——合格格落盘、不合格 key 原地留在 `RESERVED_SLOTS`（不预载、上屏走 emoji 回退，破不了断言），攒够就单出补出小表，台账清零才算该类进版完成 | 本文 §2.1 + STYLE-SPEC §8 流程第 5 步 |
| D18 | **先问"这个位置该不该有图"，再问"图好不好看"**（第二批 10 格只上了 4 格，其余 6 格全卡在槽位判断上）：① 随文小状态符（`›` 链尾、`✓` 勾选、`✅` 已领）留字符层——它跟文字色与基线走、还要随布尔值显隐，128px 位图缩到 10~14px 只会更糊，而且贴图一旦挂上就不随 `.on`/`.sel` 变（内联样式压过类规则）；② 同位重复不出第二份图（日历＝签到键、商店帐篷＝底部页签，两处都已有图）；③ 好友/放大镜缺的是界面不是美术。**判据前置到出表前**：先在 §9 找到"哪个 CSS class 的哪个位置"，再进 `RESERVED_SLOTS`，否则整键不排产 | STYLE-SPEC §9「有些槽位不该出图」 |
| D19 | **切片档要量"真正要用的那张切片件"，不能只量参考图的比例**：`bar` 档上一轮是从基准图2 的进度条带按「描边:条高 ≈1:9」推成 `8 fill` / 2px 的，隐含前提是"圆角很小"；r12 实际出图的底槽圆头有 17px 宽，照 8 切会把圆头划进中段横向拉成直线。用 `tools/measure_nine.py` 量切片件（柱高爬到满高 98% 所需列数 = 圆头宽）才拿到真值，改成四边分设 `10 17 10 17 fill`。**同一个坑也适用于宿主清单**：§9 原先粗写的"主城 11 条 + HUD 5 条"逐条量下来是"HUD 4 条达标、主城 0 条"，其中 6 条根本没有代码建这个 DOM（死样式）、2 条名不符实 | STYLE-SPEC §9 `bar` 档实测依据 |
| D20 | **接线要验"贴图真的上了屏"，不能只看代码挂了 key**：第二批把 HUD 两键接成 `_assetBgUrl` 一次性取 URL（照本文件邮件行的旧例），但 DomHud 的整棵 DOM 是在**场景加载时**建的，那一刻 AssetLib 的异步预载常常还没回来 → 返回 null、glyph 永久留着当占位，check 全绿、构建全过、图在包里躺着没人用。邮件行没暴露这个问题是因为它随面板打开才建。主城与登录两层本来就有 `_pendingTex` 队列，HUD 独缺——现补同款队列并在 `update(dt)` 里每帧排空。**判据**：无头烟测打的按钮文字表里，接了图的键 textContent 应变空（`hudBtn pauseBtn|`）；还是 `\|❚❚` 就是没上图 | `DomHud._tex/_flushTex` + `tools/smoke_headless.mjs` |
| D21 | **同一张九宫格件给不同厚度的宿主用，切片值不变、显示宽度必须跟着缩**：`bar` 档的 4px/7px 是按 HUD 的 20~28px 条高从 52px 源件折算来的，直接套到主城 12px 的条上，上下两条板边就吃掉 8px、只剩 4px 内腔，等于把一根细线糊成一坨。故同切片再加一档 `barThin`（2px/4px，按 12÷52≈0.23 折算）。**推论**：`≥10px 才可贴` 这条筛子真正要保证的是「内腔还剩得下东西」，10px 是配 `barThin` 才成立的门槛，不是件本身能无损缩到 10px | `UiPlate.NINE` + STYLE-SPEC §9 薄板档 |
| D22 | **宿主要在图上叠字，出图时就得把那块位置"要出来"**：`.statRank` 的名次数字不能丢（丢了第 1 与第 3 就只剩颜色之差），所以 r14 的 prompt 加了一句功能约束「正中留出一块干净的空位，那个位置要叠名次数字，不要画任何图案」。这不是替模型设计形象（§8 禁的是「金色勋章」这种审美描述），是把宿主摊不开的事实告诉它。**两个连带后果**：① 模型把"空位"做成了**镂空**，切片件放棋盘底上看着像抠穿了一块——目检时必须先读过台账再判合格，否则会误杀整表；② 图上屏后 `icon()` 要带 `keepGlyph`（否则数字被当占位符摘掉），并且字色/字号都得跟着改（`color` 换金属色、字号 34→20 才塞得进环），这三样只在贴图真的到位时才改，缺图就整组回退原样 | STYLE-SPEC §9 名次奖牌行 + `DomHud.RANK_MEDAL/_rankTex` |
| D23 | **美术进版改成「一轮一类、一类一次出齐、出完自测」**：用户点名原来的做法"每次只做几个、不按类别、进版很乱、界面完全不行"。新口径固定四步：① 先按类别列清单（按钮类 / 图标类 / 角标类 / 资源类 / 战斗件），列之前先跑界面普查拿实测尺寸；② 一类一张表一次出图，不合格的格子下一批补，不为一件单独发一次请求；③ 整类接一个契约表（如 `CITY_BUTTON_PLATE`）而不是十几处手工接；④ **每类由我跑无头取景自测并当场截图判合格，不再给用户出手测清单**——只有真机手感类问题才请用户验。连带：贴不上、或贴了反而更难看的位置**显式写 `key: null` 留在表里**（附实测理由），比悄悄不接更能防止下一轮重复踩 | 本文件 §6「Step1 按钮类」+ `tools/page_shot.mjs` |
| D24 | **无头自测要能看见页面的声音与状态，否则「没截到」会被当成「没有」**：Step2 给 HUD 加精英词缀徽标，连着四轮取景都是「整行收起」，差点下结论说宿主没生效。真因有两条，都藏在看不见的地方：① **`Runtime.consoleAPICalled` / `exceptionThrown` 默认不冒到 stdout**——烟测脚本只发 CDP 命令、从不监听事件，页面每帧抛错也一声不响；现给 `smoke_headless.mjs` 接上事件订阅（异常与 error/warning 去重限量打印）。② **战斗会卡在「团队升级三选一」上**——那一刻 `#domHud` 挂着 `paused` 类、波次与击杀数字全停，取景读到的 `alive=0` 其实是"整局暂停"而不是"没有精英"。现每轮探测带上 `paused`，为 Y 就点一张卡把它推下去。加上这两条后同一次运行就在第 4 波目击到徽标亮灯（`bg=Y`、emoji 被摘）。**推论**：自测报「未观察到」之前，先证明观察手段本身看得见 | `tools/smoke_headless.mjs` 事件订阅 + 波次探测 |
| D25 | **契约表接管一类之后，"接了没有"就不再取决于谁记得调用**：Step1 把主城按钮族收进 `CITY_BUTTON_PLATE` 后，铺板只在 `_switchPage` 尾部跑一次；Step2 给背包加检索框，打一个字触发 `_refreshHeroes` 就地重绘——整页 DOM 换掉，那一页所有板子跟着消失。取景截图对比才发现。`_applyPendingTex()` 看着像通用收尾，其实 `HomeUiPlay` 一次都没调，逐处补一行迟早漏一处。改成 `MutationObserver` 盯 `#homeUi` 子树增删、有重建就在下一帧重扫，且**只观察 `childList`**（铺板写的是 style，属性变更不进名单，不会自激）。同时把扫描范围从整个 `#homeUi` 收到 `.viewport`：弹层 CTA 归 `_openPop` 的 kind 路由，两套规则不再互相覆盖 | `HomeUiMall._watchCityPlates` + `HomeUiCore._plateCityButtons` |

## 6. 进度跟踪

| 批次/阶段 | 状态 | 完成标记 |
|---|---|---|
| P0 工具固化 | ✅ 2026-09-20 | `--rect` 落地（slice_sheet fit 不拉伸）+ 无头 CDP 烟测脚本 smoke_headless.mjs |
| 批0 直取框徽 | ✅ 2026-09-20 | 14 件入库（fresh 绿幕表 r6_frames/r6_badges，非基准图直取；在库备用未上屏） |
| P1 主城与弹层（批1~3） | ✅ 2026-09-20 | 批1 r6_panels（panel_main/sub 九宫格接 .pop、ribbon_banner 接 popBanner、bar_title 接 popTop）；批2 r6_btns（btn_danger/btn_video/btn_round 接 CTA+popBack）；批3 r6_icons（ico_notice 接公告钮+DomHud 邮件行、trophy/lock 备用） |
| P2 战斗主循环（批7~8） | ✅ 2026-09-20 | 批8 road/escort 重生成、批7 五怪物 trim-tight；无头烟测进战截图过（批6 四英雄+指挥官同轮入库） |
| P3 内容图标（批4/6/10） | 部分 | 批6 ✅；批4 状态图标无接线目标本轮跳过；批10 用户豁免不换 |
| P4 资源进度与特效（批5/9） | 未开始 | 无接线目标/动 CSS 骨架风险高，留待下轮 |
| 规范固化轮（进版前置） | ✅ 2026-09-20 | UiPlate 契约层（语义→板 / NINE 切片档 / icon 摘 glyph）；`PLATE` 取代文案正则、`ad` 显式化；禁用态改「同板 + CSS filter 派生」；品质框 frame_q* 上屏 `.popQ .qi`；`RESERVED_SLOTS` 103 项预留进代码＝采购单（预载跳过）；checker 从 10 组扩到 16 组（加「代码引用扫描扩到所有槽位字面量」「UiPlate↔STYLE-SPEC §9 对账」「禁止页面手写 borderImage」「预留必须进 MANIFEST」）；揪出幽灵引用 `monsters/crawler` 已删 |
| P0 在库件接线 | ✅ 2026-09-20 | 新增 `UiPlate.frame()`（border-image 无 fill，框与宿主背景共存）+ `PopRowOpts.frameTex`；avatar_frame→个人主页名片行、btn_round2→帮助 ? 钮、ico_task→侧栏任务、ico_trophy→排行/排行榜、shop_scroll→图鉴/怪物图鉴、shop_gift→侧栏礼包、shop_chest→商城每日免费；`mkBtn/mkFoot` 增贴图槽（后续新图标传 key 即可）；侧栏 `.ic` 两层补齐 box 尺寸使图与字形同 footprint；checker 17 组（加「在库件必须有归宿」+ 修清单自引用/块注释两个口径漏洞），退役候选 10 件入表待拍板 |
| 全绿基线（批量替换前置） | ✅ 2026-09-20 | 12 条历史 FAIL 归零：`check-mail` 4 条定长窗口断言改 `fnBody` 函数体提取（签名已演进为 `_openMailModal(openId)`、`mailClaimAll` 类已废）；`check-split` 2 条落位锚点同步现实（`_buildNoticeBar` 随走马灯删除→改盯 `_buildTopbar`、`_openTuningModal(onBack?)` 签名），行数欠账改 `SPLIT_DEBT` 棘轮；`check-hud-slim` 4 条按现版 HUD 重写（顶栏 122px + `--safeTop` 双保险、levelBadge 独立胶囊、bossBar 让位 139px），并把裸 `.xpRow` 反向断言收进 `#domHud` 前缀（同包主城同名类会误命中）。批量替换期自此每轮 FAIL 数即真回归数 |
| 真机反馈修复（板压字 / 锁徽压字） | ✅ 2026-09-20 | 用户截图指出两处：① 英雄养成五入贴蓝板后文字压在板面上——44~56px 小方键本就不足板厚四倍，撤板回 CSS 底色，主城板族范围收窄到 `.btn.big`；顺带查出主城没有 `--pu` 导致板厚恒 10px 不随手机缩放，`.btn` 两层补齐；② 编队条未解锁行卡的 🔒 沿用大头像条的 `translate(32px,-80px)` 摆位，压在名字上——改 `.hero-roster .hpick.lock::after` 为随行内联（两层，靠特异性压过通用规则）。规范同步：§9 薄板档补实测依据与闸门、D10/D10b 改写 |
| 图标类进版三批（批3 主体） | ✅ 2026-09-20 | 三批共 23 个候选键：**19 件出图并接线**（第一批 12 走 `mkBtn`/`.ic` 槽；第二批 `ico_ad/slider/pause/stats` 走商城看广告键、设置音量行、HUD 左上两键；拆行轮 `ico_sound/mute/info` 走设置行图标列与 HUD 小节头，另在 HUD 危险操作头复用一次 `ico_warn`），**7 件撤键**（`ico_more`/`ico_check` 随文小状态符、`ico_calendar`/`ico_shop`/`ico_inbox` 同位重复、`ico_codex`/`ico_loot` 拍板保留旧图），**3 件留采购单等功能**（`ico_friend`/`ico_undo`/`ico_search`），**1 件假宿主**（`ico_empty`）。逐格判决见 §2.1，槽位判据沉淀成 D18 |
| 二级页签族（批1 追加） | ✅ 2026-09-20 | r13 一张 3列2行表出齐 6 格、棋盘底目检 6/6 合格：**5 件落盘接线**，吃下 7 个位置（商城 `.shopTabs` 四签 + 背包 `.bagTabs` 四签，装备/宝石/材料三签共用）；两套页签各建 `span.ticon` 图标位、尺寸 20px 归两层 CSS、选中态仍走 CSS（§10）。**`tab_core` 图合格但没落盘**——规范写的宿主 `.chTabs` 是死样式（护送页早已换成章节头 + 翻页箭头），背包第四签真名是「道具」，键留采购单等一个真存在的核心分类。两条自我更正进 §9：宿主从三套改两套、图标尺寸从抄来的「20~34px」改成实测 20px |
| 名次奖牌族（批0 追加） | ✅ 2026-09-20 | r14 一张 3列1行表出齐金/银/铜三档、目检 3/3 合格并全部接线。宿主按实测只剩一处（HUD 伤害统计 `.statRank` 前三档，两个建点），规范并列的另两处一条判死（排行榜那格 10~26px 属随文小符号）、一条是死样式（`.lbRank` 整块排行榜 CSS 是孤儿）。奖牌环心按 D22 出图时就留出叠字空位，数字保留并跟着换金属色墨色与缩小字号。顺带记一条对账口径：key 用拼接（`'ui/badge/medal' + rank`）会被 `check-art-manifest` 判成「代码引用未登记」而 FAIL——这不是检查器误报，正是它要防的静默占位，所以同类多档 key 一律整串字面量成表 |
| 材料与宝石族（批10 追加） | ✅ 2026-09-21 | r19 表七件落盘接线（背包格 `.bcell i` + 商城货卡 `.gIc`），`UiPlate.MISC_TEX` 立成 id→key 契约表；绿宝石要 `--tol 95` 才不被抠穿这条进 §8 坑表 |
| **Step1 按钮类（按类别批量进版的第一类）** | ✅ 2026-09-21 | 用户拍板改工作方式：**一轮一类、一类一次出齐、每类自测**，不再一次几件。第一类做法：先做界面表面普查拿到每一面的实测尺寸 → 一张 r24 表出齐 8 块板 → 4 件落盘 → `CITY_BUTTON_PLATE` 一条表接管整族铺板（商店 0→9、英雄 0→17、护送 0→20 面有板）→ 无头取景自测并当场撤掉三处贴不动的位置。自测工具两份入库：`tools/audit_ui_surfaces.mjs`（六界面全量普查出 JSON）+ `tools/page_shot.mjs`（单页取景并直接打印「哪些面已贴图」），`tools/sum_surfaces.mjs` 汇总前者。**从这一轮起，每轮收尾的自测由我跑无头取景，不再出手测清单** |
| **Step2 图标类（一类一次出齐 + 缺落点就建落点）** | ✅ 2026-09-21 | r21（状态与属性 10 格）与 r23（功能补件 4 格）两张表落地，**11 件进版、3 件按判据不落盘**（`ico_undo` 无可撤销操作、`status_ice`/`status_poison` 无对应机制，切片件归档 `art-spec/reference/stock/ico/`），另撤掉永远不会有人填的 `ico_skill`。新建落点三处：背包按名字检索框、行动页好友入口（功能未开放但弹窗有说明有出路）、`_popEmpty` 默认空态件；新建宿主两处：战斗 HUD 波次 chip 底下的本波精英词缀徽标行、装备详情/重铸弹窗的词缀行贴图槽。**自测两处收获**：① 第 4 波真的拍到火焰精英徽上图（`gen-output/smoke_step2p/06b-affix-badges.png`）；② 拍到一条真缺陷——就地重绘会抹掉整页按钮板，已由 `MutationObserver` 收口（D25）。烟测脚本补上页面异常订阅与「升级面卡住」识别（D24） |
| **Step3 角标/星/节点类（一类一次出齐 + 判形不判在不在）** | ✅ 2026-09-21 | 这一类分两张表处理，结果一成一败：**评价星 2 件 + 天赋节点 3 件落盘接线**（r17b 表，切片要 `--dilate 2` 才不渗绿边），**角标与徽章 5 件整表不落盘**（r25 表五格全部平整合格，但模型出的是方形/六角/星芒徽章，宿主是 21~34×13~18 的扁角标和 27px 整宽胶囊——硬贴要么压成饼要么盖字，切片件归档 `art-spec/reference/stock/badge/`，键留采购单并写明实测轮廓）。新规则进 §9：**出图前除了量宿主尺寸，还要写下宿主的轮廓形状**。两处接线都把"没有元素可贴"的旧实现拆成元素：图鉴威胁等级从 `'★'.repeat()` 文本换成 `_starRow` 逐颗星、天赋格从 CSS 边框三态改走 `_popGrid` 新增的 `tex` 槽。**自测抓到自己的假失败**：`#homeUi` 在隐藏态被程序点击照样建 DOM、贴图挂得上但宽度全读 0，据此报"星没上图"是错的；改成先真退出战斗再等页面可见，量到 `w=36` 并拍到两张证据图（`gen-output/smoke_step3d/08-talent-nodes.png`、`09-bestiary-stars.png`）。升星弹窗那排同一星条本轮不做——`HomeUiHeroes.ts` 已顶到 2490 行预算，拆文件排在接第二处之前 |
| **Step4 资源与载具类（量的不是尺寸，是可见性）** | ✅ 2026-09-21 | r26 一张 4列1行表出齐章节载具 3 件 + 英雄碎片 1 件，**四件全部落盘接线**。载具的宿主是这一轮真正的工作：护送页场景里那块 `.veh` 在浅色（手机）主题被整块 `display:none`，贴上去真机永远看不见，所以落点开在章节头左端（一章一件、按 `CHAPTER_THEMES[].veh` 换图），它反而是手机上唯一看得见载具的位置。碎片不进顶栏胶囊——碎片是四英雄各自的库存、不是能加总的货币，摆个总数是骗人，改接升星弹窗的说明行。**这一类也是「没实现的功能也要用起来」这条令的边界**：勋章/能量/招募券三件整批不出图，本作没有这三种资源，挂上顶栏就是三个恒为 0 的假数字，破「无死键」红线（理由写进 `RESERVED_SLOTS` 那三行，经济扩表当天照行施工）。顺带解开 Step3 那处"等拆文件"的星级：把助手建在有余量的 Mall 层、调用点只改一行，棘轮一格没加。自测：载具牌 `bg=Y、39×39`、碎片行 `bg=Y、49×49` 两张截图目检通过（`gen-output/step4_escort.png`、`gen-output/smoke_step4/11-frag-row.png`），并拍到取景脚本自己的坑（只点 `.popClose`、截图停在上一层弹层） |
| **Step5 战斗 UI 件与剩余弹层底板（一类里判死一件假宿主）** | ✅ 2026-09-21 | r27 一张 3列1行表出齐战斗 HUD 三件并全部接线：**读数牌底板**（波次与击杀两枚 chip 共用一块木牌，切片为横长件新设百分比档 `chip`，因为 `measure_nine` 的圆头读数在这种带装饰包边的件上是失效的）、**技能槽底托**（本轮第一件画布层贴图：`AbilityBar` 原来全靠 `Graphics` 画底，现按该文件既有的"美术就绪即应用一次"探测挂一枚 `Sprite`，缺图整节点不启用，冷却遮罩与充能环的半径口径一行没动）、**首领徽**（把 `👑 名字` 那串文本里的皇冠拆成独立元素才贴得上，与 Step3 那串 `'★'.repeat()` 同一个失效模式）。**同一类里判死一件**：`panel_mini` 上一轮的挂单理由是"跟 `.frame` 的装饰伪元素打架"，本轮复核发现真正的理由是它的两个建点 `_openSheet`/`_openResult` **零调用**——那是"全部二级界面已迁新弹层"那条棘轮留下的桩子，删不得也用不上；我把板接上去跑了一遍检查器，「在库件必须有归宿」照样绿，于是补一条判据进 §9：**死方法里的引用在检查器眼里算宿主，核宿主先核谁调用这个建点**。自测：波次与击杀两枚 `borderImage=Y、61×44`、BOSS 波那枚 `bg=Y、15×15`（脚本改成等 `.bossBar` 亮灯才收工）、底托看战斗截图，顺带拍到第 4 波的「治疗」词缀徽（Step2 那族又验一种态）；Core 靠把 `_popWarn` 挪到 Mall 还掉 5 行（2239→2234）。⚠ 一处如实记下：装备词缀行的 `_popAttr.iconTex` 与卡面行内星都要"有带词缀装备 / 有重复英雄"的存档才看得见，新档量不到 |
| **Step6 战斗表现件（画布件的自测要另找证据）** | ✅ 2026-09-21 | 收尾类：r28 弹道两件（狙击 / 辐射，辐射必须 `--tol 95` 才不被当背景抠掉）、r29 丧犬 6 帧走帧（没有参考视频，改用图生图定身份 + 品红底 + 现成的 `slice_walk_sheet.py` 打包，正好落在 walk 的默认帧数上所以零代码改动）、r30 车尾受损态（图生图自完好件、`tools/crop_letterbox.py` 裁回原 4:1 保证两态比例一致、`Vehicle._syncDamageArt` 在耐久 ≤25% 时与完好件互换，门槛跟 HUD 那条 `.danger` 取同一个数）。**这一类真正的产出是"画布件怎么自测"**：DOM 那套读 `backgroundImage` 在这里全废，弹体靠 `Bullet.ts` 自带的「[Art] 弹体正式贴图生效」诊断行（取景脚本放行它）、走帧靠"同一屏两只狗摆位不同"这张放大截图、受损态靠把耐久打到 0 之后**先把结算面从 DOM 里摘掉再拍**（不然整屏被盖住）。**同批判死 9 件**：关卡主题背景 4、特效 4、激光束——逐条 grep 过，全工程连拼 key 的地方都没有，这九件缺的不是图是表现代码，先出图只会得到九个没人引用的文件。至此采购单上剩的 25 个键**每一个都写着为什么不买** |
| **Step7 容器底板族（先普查再出图的那一轮）** | ✅ 2026-09-21 | 六个 Step 类别走完之后的收尾轮：跑一遍全量表面普查（`tools/audit_ui_surfaces.mjs`），把"还在用 CSS 纯色/渐变的面"按页、按面积列全，再按可见度决定出什么。r31 一张表出 7 件（导航格两态 / 二级页签 / HUD 功能钮 / 装备槽 / 建筑卡 / 入口卡），接线走新表 `UiPlate.SURFACE_PLATE` + 四个新切片档（`navTab` `seg` `sq` `bigCard`）。**这一轮顺带把 `--tight` 的第二版坑填了**：tight 的留边原来按最长边算，横长板件上下又垫出 12% 的透明边，等于刚修的坑换个尺寸再踩——改按短边算，七件 `trim_alpha.py --check` 全过。另产出一条通用机制：`nineSlice` 贴上图那一刻给宿主打 `.plated`，CSS 靠它把"浅底深字"翻成"暗板亮字"（四处），缺图回退时旧配色照旧对 |
