# 二级浮窗 UX 布局稿（L2–L5）

入口：[popups.html](./popups.html)。独立 HTML，直接打开即可，无需服务或网络资源。

一级页稿见 [REVIEW.md](./REVIEW.md) 与 [index.html](./index.html)，本轮不重复。

## 为什么是「五段骨架」而不是网页信息流

正式项目的浮窗不是网页卡片，是操作台：进来看一眼参数，做一次决定，退出去。竞品《向僵尸开炮》的五张截图（邮箱两版、装备详情、一键合成、强化台）也是同一个形态——顶部标题或品质头固定，说明/页签固定，中间只有一段可滚，底部消耗与命令固定。

所以本稿抽出的不是样式，是段的顺序与固定/滚动的分工：

| 段 | 承载 | 是否滚动 |
|----|------|----------|
| ① 头部 | 标题，或品质头（4 级灰阶 + 品阶 + 等级） | 固定 |
| ② 说明·页签 | 一句话说明 / 页签 / 筛选条 / 进度条 | 固定 |
| ③ 滚动区 | 列表、格子、对比块 | **唯一滚动轴** |
| ④ 消耗·槽位 | 货币消耗，或材料/装备槽位 | 固定 |
| ⑤ 命令区 | 主命令 + 次命令 + 一行后果说明 | 固定 |

一个面可以缺段（S 档确认框只有 ①③⑤），但**不得有第二根滚动轴**，也不得把 ④⑤ 放进 ③ 里跟着滚走。这是与网页稿最大的区别：网页可以整页滚，游戏不行——用户滚到一半时消耗和「合成」必须还在原位。

## 四档尺寸与五级分层

四档尺寸沿用正式项目 `HomeUiStyle.ts` 的定位口径，不是重新发明的：

| 档 | 定位 | 本稿实测（390×844） |
|----|------|---------------------|
| S | `left:50%`、`top:30vh`、定宽 287 | x52 y253 **287**×260 |
| M | 左右 23、上下 18vh | x24 y152 **343**×539 |
| L | 左右 17、上下 14vh/13vh | x18 y119 **355**×615 |
| XL | `inset:0` 满屏二级页 | x1 y1 389×843（手机框 1px 边） |

M 档与正式项目实测值（邮箱弹层 x23 y152 344×540）差 1px，来自手机框边框，不是口径差异。

五级分层：L2 满屏二级页（工坊、钻取底页）、L3 普通弹窗（z220）、L4 半屏抽屉（结算）、L5 结果演出（招募，遮罩更暗且不可误触关闭）。遮罩覆盖整个 844 画框（含信息栏与底导），与正式项目 `.protoMask { inset:0 }` 同口径。

## 18 个面

四类母版，取自正式项目真实存在的浮窗，不是编出来的：

- **母版 A 列表领取**：邮箱 L3、公告 L3、任务 L3、签到 L3
- **母版 B 详情强化**：装备详情 L3、建筑详情 L3、技能详情 L3、强化台 L2（XL）
- **母版 C 选择确认**：一键合成 L3、宝石挑选 L3、装备工坊 L2（XL）、编队 L3、二次确认 S
- **母版 D 结果演出**：招募结果 L5（XL）、战斗结算 L4
- **分层与尺寸**：钻取 L2→L3、四档尺寸对照、状态与红线

## 无死键

正式项目的按钮不允许出现「点了没反应」。本稿同口径：

- 不出现原生 `disabled`（原生属性会吞掉点击，用户不知道为什么不能点）。不可用一律走 `.off` 压暗 + `data-why`，点下去在提示条里说明原因。
- 消耗行区分足量（`.ok`）与缺量（`.lack`），缺量时按钮压暗并给出差值，例如「强化石不足（缺 7）」。
- 遮罩点击也有出路：L3/L4 关闭，L5/XL 给出「本层需用底部命令关闭，避免误触中断」。
- `check-popups.mjs` 静态证明每个 `data-act` 都有交互分支，且没有孤立分支。

## 竞品参考落到稿里的三条硬规矩

1. **列表行卡**：图标 + 标题 + 标签 + 副标题 + 尾部动作位，未读用右上角标点，不用颜色区分。
2. **品质头**：4 级灰阶 + 品阶标签，不依赖颜色也能读出品质。
3. **对比块**：强化/升级给出「当前 → 强化后」两值，与强化台截图一致。

## 验证记录

### 代码级：40 项断言

```sh
node ux-layout-review/check-popups.mjs
```

覆盖：目录/尺寸表/说明表三者一一对应、五段骨架齐备且每段至少两面复用、每面都有 ①头部与退出通道、四档尺寸与五级分层、L5 不可误触关闭、滚动轴只在 ③、无哑键（每个 `data-act` 有分支、无孤立 `case`）、无原生 `disabled`、压暗必带原因、遮罩有出路、组件命名沿用正式项目、四档定位口径与项目一致、片段型组件不重复包 `art`、宝箱行固定 4 列不折行、提示文案跨重绘保留、控制器用 `onclick` 属性挂载（避免监听叠加）。

### 浏览器实测（390×844 / 360×640 / 430×932）

18 面 × 3 档共 54 组全部通过：无裁剪（首尾子块都在外框内）、无原生 disabled、①头部与退出通道齐备、宝箱行均为单行。

交互链路实测：

- 邮箱领取：`一键领取（3）` → 单条领取后 `（2）` → `全部已领`，提示条文案正确且跨重绘保留。
- 页签切换：公告 `0.9.0 版本更新公告` ↔ `限时特惠 · 钻石直购`。
- 选择合成：点选 2 件后汇总条变为「已选 2 件」，命令由「选择材料」（压暗）变为「合成」。
- 未达门槛：任务宝箱「活跃度未达 60」、签到「未到该日」、工坊 11 处压暗项均可点并给出原因。
- S 档确认框两条出路：`取 消` / `确认下阵`。
- 遮罩点击：L3 关闭（「已关闭弹窗」），L5/XL 给出解释。

### 本轮修正的六处缺陷

1. **提示条被重绘清掉**：`claim`/`cta` 先调 `feedback()` 再 `render()`，而 `render()` 整块重写 `.phone`，提示节点被替换，文案丢失（按钮点了没反馈）。改为文案存变量、由 `feedbackHTML()` 回填。
2. **提示条压住命令区**：原来贴屏幕下沿，正好盖住 ⑤ 命令区。改回正式项目 `.toastEl` 的屏幕中央口径。
3. **消耗行露出字面量 `"/>`**：`costRow` 内部又包了一层 `art()`，把已构建的 `<svg>` 片段当图标名。统一为「片段型组件收 art 片段，`cell` 收图标名」。
4. **返回键飘到面板中央**：`popBack()` 直接挂在 `.pop` 上，`position:absolute; top:50%` 以整块面板为参照，落在 y431 压住正文。移入有定位的头部块，现落在 y100（头部 25–205 内）。
5. **宝箱行折行被压**：进度宝箱行原用 `.popFixBar`（`flex-wrap:wrap`）+ 内容宽度浮动，折成两行后下沿被命令区压住。抽出 `chestRow()` 固定 4 列，任务页与结算页共用。
6. **六个面缺 ①头部**：邮箱、公告、任务、签到、一键合成、四档尺寸原先直接从 ② 或 ③ 开始，连关闭键一起没有，用户只能盲点遮罩退出。已补 `banner()`/`topbar()`。

### 实施到正式项目

本稿已按上面同一套骨架实施进 `zombie-shooter`。正式项目的弹层引擎（`HomeUiCore._renderPop`）
本来就按五段式搭好了，所以实施的重点不是重写，而是把稿里的两条红线补齐：

### 一、补齐 22 处死键

盘点发现 38 处 `disabled` 里有 22 处只压暗、点了没有任何反应——正是稿里要禁的「死键」。
逐处补上 `onDisabled`，按缺口给具体说明，而不是笼统一句「不可用」：

| 位置 | 缺口 | 点下去说 |
|------|------|----------|
| 邮箱领取附件 | 已领取 | 该附件已领取过了 |
| 体力·看广告 | 体力满 / 额度用完 | 体力已满，先消耗一些再来领（分两句） |
| 体力·钻石购买 | 钻石不足 | 钻石不足 · 还差 💎 20 |
| 商店礼包 | 今日已购 | 今日购买次数已用完 · 明日 0 点重置 |
| 天赋加点 | 满级 / 点数不足 / 前置未满 | 复用页内已有的 `hint` 文案 |
| 天赋洗点 | 未投点 | 还没有投入任何天赋点 |
| 招募免费次数 | 额度用完 | 今日免费招募额度已用完 · 隔日重置 |
| 装备合成 | 未选 / 金币不足 | 请先在上方点选一组装备（或给差额） |
| 装备分解 | 未选装备 | 请先在上方点选一件装备 |
| 材料使用 | 数量为 0 | 「xx」数量为 0，无法使用 |
| 宝石镶嵌 | 金币不足 | 金币不足 · 镶嵌需 🪙 N，还差 M |
| 武器核心嵌入 | 金币不足 | 金币不足 · 还差 🪙 N |
| 武器强化 ×2 | 强化石 / 金币不足 | 按差额说（抽出共用的 `wWhy()`） |
| 装备强化 ×2 | 未装备 / 满级 / 合金 / 金币 | 按缺口说（抽出共用的 `upWhy()`） |
| 英雄升星 | 满星 / 碎片不足 | 碎片不足 · 还差 18 片 |
| 技能升级 | 未解锁 / 满级 / 核心 / 金币 | 按缺口分四种说法 |
| 副本选中 | 已在查看 | 已在查看「xx」 |
| 远征选人 | 未解锁 / 上阵中 / 远征中 | 分三种说法 |
| 每日签到 | 已签到 | 今日已签到 · 明天再来 |
| 载具改装 | 满级 / 门槛未达 | 复用 `gate.reason` |

差额一律给数字（`还差 🪙 500`），不是只说「不足」。两处成对的按钮抽出共用的
`wWhy()` / `upWhy()`，避免两条分支各写一份判断而说法漂移。

### 二、出口补全

- **`.popQ` 缺 `position: relative`**（真 bug）。✕ 是 `position:absolute; top:50%`，
  品质头没有定位时会以整块 `.pop` 为参照，落到面板垂直中心压住正文——就是本稿在
  草案里踩过的同一个坑。修前 ✕ 落在 y≈430（面板中心），修后落在 y=188（品质头 160–246 内）。
- **L5 结果层遮罩点击静默**。引擎默认「L5/XL 不点关」，但没给出任何说明，点了没反应等于死键。
  现改为统一提示「本层需用底部命令关闭，避免误触中断」。
- 两处 L5 结果演出层（招募结果 / 礼包结果）显式设了 `maskClose: true`，**保留不动**。
  一度按本稿「L5 不点外部关闭」的口径去掉了，但项目里已有一条断言要求它们可点关，
  且理由更硬：奖励在弹层出现前就已入账，演出只是回顾，允许快速收起比强迫看完动画更省摩擦。
  引擎默认值服务「不该被误触跳过的层级」，这两个层不属于此列，属正当例外，已回退并在两处补注释。
- XL 满屏页的遮罩实测点不到（`.pop` 铺满整屏，`.protoMask` 无内边距），
  不需要额外出口，但上面的提示分支仍是兜底。

### 验证

```sh
node tools/check-popup-ux.js      # 23 项断言，失败 0 项
node tools/tc.js                  # TOTAL 0
node tools/check-ux-refactor.js   # 248 PASS / 0 FAIL
```

浏览器实测（构建后 390×844，真实点击）：

- 体力弹层两个禁用动作分别报「体力已满，先消耗一些再来领」「钻石不足 · 还差 💎 20」。
- 品质头弹层 ✕ 落在 y=188、在品质头矩形内（`closeInsideQ=true`）。
- 全量遍历 5 个一级页入口打开的弹层，点遍每处压暗按钮：14 次点击全部有反馈，
  0 处无反应。唯一被自动脚本标红的「未解锁」按钮，实测是打开「副本未解锁」拦截层
  （带「知道了 / 前往关卡」两条出路），是脚本只认 toast 的误报。
- 所有弹层都带可见出口（✕ 或 ‹），0 处只能靠盲点遮罩。
- L5 礼包结果层：奖励已入账，点遮罩可直接收起（保留项目的例外口径），「收下」同样正常关闭。
- 拦截层「知道了」正确回退到下一层的资源副本 L2 页。

### 说明

`check-popup-ux.js` 里「禁用按钮全部带解释出口」这条是按**花括号配平**解析每个
`disabled:` 所在的对象字面量，再要求同一对象内有 `onDisabled`/`onBlocked`，
而不是靠正则数距离——后者加几行就会漏判。

图标与美术仍是占位（emoji/SVG），美术样式与资源按约定留给下一轮。

## 尚未完成

- 图标、物品、角色均为灰阶体块占位（`<symbol>` 描边集），**不代表美术方案**。品质用 4 级灰阶代替颜色。美术样式与资源是下一轮独立任务。
- 英雄名、商店货架、道具数值沿用占位口径，不按正式项目逐项回填。
- 钻取层级只演示了 L2→L3 两层叠加；更深叠加（L3→L4→L5）未单独出稿。
- 未验证真实触屏设备上的滚动手感与遮罩误触率。
- 正式项目侧只补齐了死键与出口这两条红线；稿里其余版式细节（如 S 档 287 定宽、
  对比块的 `▶` 两侧留白）未逐项比对，因为它们本来就已按同一套口径实现。

## 改动范围

- 设计稿：只修改 `ux-layout-review`（`popups.html` / `check-popups.mjs` / 本文件）。
- 落地：修改 `zombie-shooter/assets/scripts/ui/` 下 6 个文件（Core / Mall / Heroes / Play / Base）
  与 `HomeUiStyle.ts`（`.popQ` 定位），新增 `zombie-shooter/tools/check-popup-ux.js`。
- 没有读取 `ux-redesign` 中的旧设计稿。美术资源未动。

